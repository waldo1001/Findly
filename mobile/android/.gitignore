# Gradle
.gradle/
build/
local.properties
# Not committed: this repo's dev/CI sandboxes had no verified JDK/Gradle toolchain to produce a
# trustworthy wrapper jar (mobile/android/README.md's Build section). Generate locally with
# `gradle wrapper --gradle-version 9.4.1 --distribution-type bin` when you have one.
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.kotlin/

# Signing — never commit keystores
*.keystore
*.jks
keystore.properties

# Misc
captures/
.externalNativeBuild/
.cxx/
google-services.json
