# Implementation handoff — current status & next tasks

> **For the next session picking this repo up: read this file, then `CLAUDE.md`, then the spec your task references. Everything you need is written down; if you find ambiguity, fix the spec first (specs/README.md).**

## Status (2026-07-22)

| Area | State |
|---|---|
| Specs 000–009 | ✅ Complete and normative — **009 device runtime (2026-07-25)** closes the last spec gap before the apps can actually track on a phone (capture policy, durable queue, per-platform scheduling, pause, push routing, geofence lifecycle, permissions). — product, full API contract, storage schema, both clients, **temporary groups (005)**, **phone-only auth (006)**, **public join links (007, 2026-07-22)**, **privacy endpoints (008, 2026-07-25 — O7 resolved: export + account/family deletion + web deletion page)**. 001 error catalog is 27 codes; `maxGroupMembers` is now **200** (D15); 006 SMS policy has **family/open operating modes**; 007 owns the `https://{JOIN_LINK_HOST}/g#CODE` surface; 001 §13 owns the privacy wire shapes. |
| Repo scaffold, CI workflows | ✅ **All 4 pipelines green on `main`** — `backend` (test+mutation+deploy), `android`, `ios`, `web-join` (see H1 CI entry below). **2026-07-22: local `main` (113 commits, the entire specs/005–007 backlog, accumulated across this whole `/dev-loop` session) was pushed to `origin/main` for the first time** — every pipeline ran for real and went green on the first try. |
| Backend | ✅ **B1–B13 all merged and genuinely deployed** to `func-findly` (previous entries below were local-only until the 2026-07-22 push). 619 unit tests + 29 Azurite integration tests, mutation 99.16%, break 60. Full groups feature (core/controls/live-locations/sweeper) + family-less profiles + per-user `Devices`/`LastKnown` re-key + the 200-member cap all live. First timer-triggered function (group sweeper) now actually running on Azure — **H3 is genuinely actionable now**, see below. |
| Android / iOS | ✅ **A1–A7 / I1–I6 all merged.** Phone-only sign-in (006), the full temporary-groups feature (005), and public join links (007: `JOIN_LINK_HOST` build config, App/Universal Links, on-device QR) shipped on both platforms; Android release-signing CI wiring in place (A7). |
| web | ✅ **W1 merged and live** — the project's first `web/` code. Join-link landing page + `.well-known` files deployed to `swa-findly` (H4, done 2026-07-22). One real bug found and fixed post-deploy: see H4 note below. |
| Azure/Firebase provisioning | ✅ Azure infra + OIDC CI/CD done; join-link SWA provisioned (H4). **Firebase phone-auth setup (H2) done 2026-07-22**, except the two pieces that are genuinely Apple-enrollment-gated (iOS App Check registration, APNs key upload — both now owned by H6, which already tracks them) and the account reset (deliberately deferred to just before real launch). Android on-device sign-in is fully unblocked; iOS on-device sign-in still needs H6. **⚠️ 2026-07-25: `stfindly` had zero Table Storage tables** (missing from both `azure-setup.md` and the rename recreate script — Table Storage does not auto-create on write, unlike blob containers) — every write endpoint 500'd until found and fixed live; all 13 tables now provisioned + both scripts corrected; see the dev-loop log entry below and **B23** (code-level defense-in-depth, self-healing table creation). |

## Rename to Findly + infra recreate (2026-07-25)

The app was renamed **Where's Waldo → Findly** (O10 resolved) and the cloud infra recreated under the new name. Full runbook: [`docs/findly-rename-runbook.md`](findly-rename-runbook.md). State:

- ✅ **Code rename** merged (PR #1); GitHub repo renamed `WhereIsWaldo → Findly`; origin re-pointed. Bundle ids `com.findly.{android,ios}`.
- ✅ **Azure recreated + verified**: RG `Findly`, `stfindly`, `func-findly`, `swa-findly`, OIDC app `gh-findly-deploy`. Fixed two script bugs live: HTTPS-only was off (now on), and the OIDC role assignments + immutable-subject federated credential (renamed repos emit `repo:owner@id/repo@id:...` → `AADSTS700213`). Script hardened in PR #4.
- ✅ **Firebase**: new project `findly-71f7b` (old `whereiswaldo-30e9c` is immutable). `FIREBASE_PROJECT_ID` + `FCM_SERVICE_ACCOUNT_JSON` set on `func-findly`; config files placed (gitignored). Apple **Team ID `92A2K3Q7NH`**. **Phone-auth console setup re-applied to `findly-71f7b` 2026-07-25** (Phone provider enabled + test number `+32 470 00 00 01`/`123456` added — the H2 console steps didn't auto-carry to the fresh project). SMS region allowlist + Blaze budget alert for *real* numbers is the remaining H2-on-new-project tail (test-number sign-in doesn't need it).
- ✅ **GitHub variables** set (6× `AZURE_*`). **Client config** (PR #3): new `JOIN_LINK_HOST = kind-plant-0fb99b003.7.azurestaticapps.net`, release `BASE_URL = https://func-findly.azurewebsites.net/api/`. **Universal Links** live & verified (AASA `92A2K3Q7NH.com.findly.ios`, assetlinks `com.findly.android`, both 200/json).
- ✅ **CI**: `android`/`ios` PR path filters removed (PR #5) so required checks always report.

**✅ DONE — backend is deployed to `func-findly` and smoke-tested end-to-end (2026-07-25).** Unauth `GET https://func-findly.azurewebsites.net/api/v1/families/me` → `AUTH_MISSING_TOKEN` (401); a garbage/unsigned bearer → `AUTH_INVALID_TOKEN` with *"no applicable key found in the JSON Web Key Set"* (proves the deployed verifier really fetches `findly-71f7b`'s JWKS and checks signatures).

**✅ Smoke test PASSED (specs/001 §11, 2026-07-25).** Minted a *real* Firebase ID token for the test number via the Identity Toolkit REST flow (`sendVerificationCode` → `signInWithPhoneNumber` code `123456`; token `iss/aud = findly-71f7b`, `phone_number = +32470000001`), then `GET /v1/families/me` with it → `PROFILE_NOT_FOUND` / 404, exactly the fresh-user signal (specs/001 §1.5). Note: the *raw REST* mint only works once the Phone provider is enabled on the project (see the Firebase bullet above); it is not App-Check-gated yet (H8 pending). The on-device Android-app path additionally needs a **release** build — the debug variant points at `http://10.0.2.2:7071` with `AUTH_MODE=insecure-local` and never touches `func-findly`.

**Open PRs:** none. **PR #2 (H7 legal pages) merged 2026-07-25** (`d2b8133`) — privacy + ToS live at `/privacy` + `/terms` on the join-link host (200, correct content type). This completes H7's authoring/hosting half.

**Apple-portal tail (H6, enrollment done):** enable Associated Domains + Push + App Attest on the `com.findly.ios` App ID; upload APNs `.p8` to Firebase; apply for Location Push entitlement (000 §O1); create App Store Connect app "Findly" + TestFlight.

## What's next (2026-07-22; roadmap added 2026-07-25)

> **The store-release sequencing now lives in [`docs/store-release-roadmap.md`](store-release-roadmap.md)** — two milestones (M1 family install via TestFlight/closed track ≈ 2–5 weeks; M2 public ≈ 4–8 weeks), five phases, critical path. Key 2026-07-25 finding: the **device-runtime layer is still scaffolded** on both platforms (no real location capture, FCM, WorkManager wiring, or map tiles; no iOS app target) — tracked as the new A9–A12 / I9–I12 backlog rows. The list below predates the roadmap; the roadmap governs sequencing.

Every coding task from specs/005–007 is merged **and, as of today, actually pushed and deployed** — the `/dev-loop` backlog is clear again. What's left is human/ops work:

1. **H4 — done 2026-07-22.** Provisioned `swa-findly` (SWA Free, resource group `Findly`), granted the existing OIDC principal RBAC, set the two GitHub repo variables, recorded the real `JOIN_LINK_HOST` (`kind-plant-0fb99b003.7.azurestaticapps.net`) in both apps' build config. Found and fixed a real live bug (AASA `Content-Type`, see Dev-loop log). Re-verified live: `/g`, `assetlinks.json`, and `apple-app-site-association` all return `200`, zero redirects, correct content types.
2. **H2 — done 2026-07-22** (see Dev-loop log for the full walkthrough). Android on-device sign-in is now fully unblocked. Only genuinely-deferred pieces remain: iOS App Check + the APNs key (both Apple-enrollment-gated, owned by H6) and the account reset (pre-launch).
3. **H3 (you) — now genuinely checkable**, not just theoretically unblocked: the sweeper is really deployed and really running on schedule. Check Azure for one logged timer-trigger run on `func-findly`.
4. **Smoke-test the live backend (001 §11)** — H2 is done, so this is actionable now: sign in on an Android dev build with a test phone number, then hit `GET /v1/families/me` with that session's ID token (fresh-user happy sign is `PROFILE_NOT_FOUND`, per specs/001 §1.5).
5. **H7 (you) — naming half DONE 2026-07-25:** the O10 naming decision is resolved — app renamed **Where's Waldo → Findly** across the whole repo (bundle ids `com.findly.*`), branch `rename/findly`. Remaining H7: privacy policy + ToS (hostable at `/privacy`/`/terms`). Firebase + Azure must be **recreated** under the new name — see **[docs/findly-rename-runbook.md](findly-rename-runbook.md)** (Azure teardown/recreate is scripted at `scripts/rename-azure-recreate.sh`; Firebase is a console redo of H2). Unblocks H5/H6.
6. **H8 — blocked on H6 now, not H2:** App Check needs to be *enforced* on both platforms before SMS regions open, and iOS App Check can't even be *registered* until H6 supplies a Team ID. Re-check this the moment Apple enrollment clears and H6's App Check piece lands.
7. **Store path (after H7):** H5 (Play, needs A7 done ✅ + H7) / H6 (App Store, blocked on the Apple enrollment in progress — also now the thing H8 is waiting on). Runnable checklist: `docs/store-readiness.md`.
8. ~~**Next design session:** the **privacy-endpoints spec**~~ **DONE 2026-07-25** — [`specs/008-privacy-endpoints.md`](../specs/008-privacy-endpoints.md) authored (O7 resolved; wire shapes 001 §13, storage 002 §4.2, decisions 000 §D17). The `/dev-loop` backlog has coding work again: **B17/B19 (then B18), W2, A8, I8** + human **H9**. Implementation is Sonnet territory (model split); B19 → B18 ordering matters (cascade reuses family-delete logic).
9. Standing items: apply for the **iOS Location Push entitlement** once Apple enrollment completes (000 §O1), create the iOS `.xcodeproj` app target (specs/004 §1.1), and the tech-debt list under `## Dev-loop log`.

The detailed original B1 checklist is preserved below for reference.

## Task B1 (DONE — original checklist, historical): backend — family creation & device registration, strict TDD

Scope: specs/001 §3.1 (create family) + §4.1 (register device) **only** — the rest of §3 (invites/members, §3.2–3.6) belongs to B3 — plus the http plumbing these two need (§1.3 envelopes, §1.5 auth guard). Everything else stays unimplemented.

1. **Scaffold the source layout** described in `backend/README.md`: `src/{functions,http,domain,ports}` + `test/{fakes,unit}`. Define the ports first (`src/ports/`): repositories, `TokenVerifier`, `Clock`, `IdGenerator` — interfaces only. Pinned contract: `IdGenerator.next(length)` returns `length` chars of `[A-Za-z0-9]`; **domain code** adds the `fam_`/`lr_` prefix; the `SeqIdGenerator` fake pads its sequence to `length` deterministically. Delete `src/index.ts` once real sources exist.
2. **RED — write these failing tests and run them (`npm run test:watch`); confirm every one fails before implementing:**
   - `test/unit/domain/createFamily.test.ts`
     1. creates family with given name; creator becomes role `parent`
     2. generates `fam_` + 20 `[A-Za-z0-9]` id via the `IdGenerator` port (assert format)
     3. writes the `Users` profile row (uid → familyId, role) via repo fake
     4. creates `Entitlements` with `subscriptionStatus: "free"`
     5. returned features === derivation from `PLAN_MATRIX.free`
     6. `FAMILY_ALREADY_MEMBER` when uid already has a family
     7. `VALIDATION_FAILED` for empty name / name > 50 chars / missing displayName
     8. records usage metric `apiCalls`
   - `test/unit/domain/registerDevice.test.ts`
     1. first registration applies defaults: `syncIntervalMinutes: 15`, `trackingEnabled: true`, `deviceName` = `model` when omitted
     2. upsert of existing deviceId updates `pushToken`/`appVersion`/`model`/`platform`, **preserves** interval/paused/deviceName
     3. `FAMILY_NOT_FOUND` when caller has no family
     4. `VALIDATION_FAILED` for platform ∉ {android, ios} and non-UUID deviceId
     5. registration without `pushToken` is accepted (token is optional, §4.1)
     6. `VALIDATION_FAILED` with `details.reason: "deviceIdInUse"` when the deviceId belongs to another user (§1.4)
     7. `LIMIT_EXCEEDED` with `details.limit: "maxDevices"` at the plan cap (count from device repo fake)
     8. upsert never counts against the cap (2 ∩ 7)
   - `test/unit/http/authGuard.test.ts` — missing header → `AUTH_MISSING_TOKEN`; verifier throws → `AUTH_INVALID_TOKEN`; expired → `AUTH_TOKEN_EXPIRED`; happy path yields `{uid, familyId, role}`; no-profile allowance for the two §1.5.3 endpoints
   - `test/unit/http/envelope.test.ts` — `ok()` always embeds `features`; `fail()` matches §1.3 with `requestId`; error codes only from the §10 catalog
3. **GREEN:** minimal `src/domain/family/createFamily.ts`, `src/domain/device/registerDevice.ts`, `src/domain/plan.ts` (PLAN_MATRIX), `src/http/{envelope,errors,authGuard,validate}.ts` + `test/fakes/` in-memory ports. No Azure imports anywhere in these paths.
4. **REFACTOR**, then run `npm run mutation` — must pass `break: 60`. Kill surviving mutants with tests, don't lower the threshold.
5. **Wire the edge (thin):** `src/functions/{families,devices}.functions.ts` (v4 `app.http`, routes `v1/families`, `v1/devices`) + the five small table adapters (`Families`, `Users`, `Devices`, `Entitlements`, `Usage` — specs/002 §2; `Usage` is required because §3.1 records `apiCalls`) + `src/adapters/auth/firebaseJoseVerifier.ts` (specs/001 §2.2, incl. `AUTH_MODE=insecure-local` guard §2.3). Adapters get no unit tests (integration later); functions stay logic-free.
6. **Verify:** `npm test` green, `npm run mutation` green, `npm run build` clean. Optional smoke: `npm run dev:storage` + `npm run dev`, then `POST /api/v1/families` with an unsigned JWT → 201 envelope.
7. **Review gate (mandatory):** code review (spec conformance + TDD evidence) **and** security review per [docs/security-review-checklist.md](security-review-checklist.md); fix all findings. `/dev-loop` runs this gate automatically; a manual session must run it explicitly before committing.
8. Commit(s) referencing the spec, e.g. `backend: create family + register device (specs/001 §3.1, §4.1)`.

**Definition of done:** every §11-checklist item touching create-family/register-device is covered; red was observed before green; mutation gate passes; the code + security review gate passed; nothing outside B1 scope got implemented.

## Backlog (single source of truth for `/dev-loop` — keep the format machine-parseable)

Status values: `todo` | `in-progress` | `review` | `done` | `blocked` | `human` | `failed`. A task is **runnable** when its status is `todo` and every ID in *Depends on* has status `done`. `human` tasks are performed by the user; only the user may flip them to `done`.

| ID | Scope | Depends on | Status |
|---|---|---|---|
| B1 | Create family + register device + http plumbing (001 §3.1, §4.1; detailed checklist above) | — | done |
| B2 | Locations: report batch §5.1 + latest §5.2 (idempotency markers, last-known only-newer, piggyback) | B1 | done |
| B3 | Invites & members §3.2–3.6 | B1 | done |
| B4 | Locate flow §6 + FCM adapter §8 | B2 | done |
| B5 | Geofences §7 (config ETag flow, events, flag-filtered fan-out) | B2, B4 | done |
| B6 | History §5.3/§7.4 (blob store read+cursor, completes B2's append-only historyBlobStore) + storage-adapter integration tests vs Azurite (002 §6) | B2 | done |
| H1 | Run `docs/azure-setup.md` (Azure + Firebase + branch protection). **Note (2026-07-21): the Firebase auth-provider portion is superseded by H2 (phone-only, specs/006); Azure/CI/branch-protection portions were done 2026-07-20.** | — | human |
| A1 | Android: write `003-android-client.md` spec **first**, then Compose foundation — **swappable design-system layer** (tokens→Material3 theme→stateless components, light+dark), full 001 API client, Firebase-Auth abstraction (stubbed), device registration §4.1, nav scaffold, one proof screen, JUnit logic tests | B1 (H1 waived for coding) | done |
| I1 | iOS: write `004-ios-client.md` spec first, then SwiftUI foundation — logic/design-system in a headless-testable **SPM package**, **swappable design-system layer** (tokens→theme→components, light+dark), full 001 API client, Firebase-Auth abstraction (stubbed), device registration §4.1, nav scaffold, one proof screen, `swift test` logic tests; **flag the Location Push entitlement application as a human/Apple task** | B1 (H1 waived for coding) | done |
| A2 | Android feature screens on the design system: live map §5.2, history §5.3, geofences editor §7.1–7.2, locate-to-request §6, device/family settings §4.2–4.3/§3.5–3.6, invites §3.3–3.4 | A1 | done |
| I2 | iOS feature screens on the design system: same inventory as A2 (§5.2, §5.3, §7.1–7.2, §6, §4.2–4.3/§3.5–3.6, §3.3–3.4) | I1 | done |
| H2 | Firebase console phone-auth setup per `docs/azure-setup.md` §3 (specs/006 §6/§8): Blaze + budget alert, enable Phone / all other providers off, SMS region allowlist BE+NL+FR+DE+LU, test phone numbers (console-only), App Check (Play Integrity + SHA-256s → refreshed `google-services.json`; App Attest), APNs key upload, `FCM_SERVICE_ACCOUNT_JSON`, one-time account reset (delete legacy users + wipe test storage). **Done 2026-07-22** except iOS App Check + the APNs key (Apple-enrollment-gated, owned by H6) and the account reset (deferred to pre-launch). | — | done |
| A3 | Android phone sign-in (specs/006, 003 §7): new `AuthProvider` (`startPhoneVerification`/`confirmCode`), `FirebaseAuthProvider` rewrite + `CurrentActivityProvider`, phone-shaped `DevAuthProvider`, two-step `SignInStateHolder`/`SignInScreen`, `PhoneNumberNormalizer`, `PhoneAuthUserMessage`, **delete the email/password path**, full StateHolder-level tests (dev-mode complete without H2; on-device Firebase verification needs H2) | — | done |
| I3 | iOS phone sign-in (specs/006, 004 §4): `AuthProviding` extension + `PhoneAuthError`, phone-shaped `StubAuthProvider` **+ real unsigned-JWT dev token fix**, two-step `SignInViewModel`/`SignInScreen`, `PhoneNumberNormalizer`, `FirebaseAuthProvider` in the app target behind the RootView seam, `AppConfig.firebaseProjectId` (same H2 caveat as A3) | — | done |
| B7 | Family-less profiles (001 §1.5, §4.2–4.3): nullable `familyId`/`role` in `UserProfile`/`AuthContext`, `PROFILE_NOT_FOUND`, four-endpoint bootstrap allowance, family-less own-devices listing + full-field own-device PATCH | — | done |
| B8 | Re-key `Devices`+`LastKnown` to PK=`ownerUserId` (002 §2.4–2.5): adapters, per-member family reads (§4.2/§5.2/§6.1/§8 fan-out), per-user `maxDevices`, §3.6 cleanup, history-append-only-with-family gate (001 §5.1) | B7 | done |
| B9 | Groups core (001 §12.1–12.3 + §12.6; 002 §2.10–2.13): `Groups`/`GroupCodes`/`GroupExpiry` tables + `Users` `group:` reverse index, derived state + lazy expiry, 6 new error codes, PLAN_MATRIX group limits + `flags.groups`, owner-plan capacity rule | B7 | done |
| B10 | Group controls (001 §12.4–12.5, §12.7–12.9): patch (extend/reactivate/end-early), delete (inline hard delete), rotate, kick, leave + `GroupExpiry` row maintenance | B9 | done |
| B11 | Group live locations (001 §5.1 fan-out side effect, §12.10; 002 §2.12): ingest fan-out to `GroupLastKnown` (active-only, position-only, only-newer) + group latest endpoint with `isStale` | B8, B9 | done |
| B12 | Group sweeper (002 §4.1, §6): **first timer-triggered function**, 45-day bucket walk, per-policy physical deletion, idempotent re-run + Azurite integration tests | B10, B11 | done |
| H3 | Verify the deployed timer trigger fires on `func-findly` after B12 (consumption-plan schedule locks use the host's `AzureWebJobsStorage`; check one logged run). **2026-07-25: timer deployed + schedule ARMED** — host logged *"next 5 occurrences of 'groupSweeper' (Cron '0 0 3 * * *'): 07/26 03:00Z…"*; 0 executions yet because deploy (12:42 UTC) was after today's 03:00 slot. **VERIFIED DONE 2026-08-02** — the awaited first natural run happened exactly as predicted, and seven more after it. Queried App Insights (`func-findly`, appId `56faa0a2-2993-401a-90ae-cdef0ab28127`) via `az monitor app-insights query`: **8 runs, 8 successes, 0 failures**, one per day at the scheduled slot — `2026-07-26T02:59:59` (the predicted first), then 07-27, 07-28, 07-29, 07-30, 07-31, 08-01, `2026-08-02T03:00:00`, all `success=true`, `resultCode=0`, durations 1.6–2.2s. The cron (`0 0 3 * * *`) fires reliably on the consumption plan, the host's `AzureWebJobsStorage` schedule lock works, and B12's sweeper is genuinely live in production. Note for future sessions: this ran unattended for a week while the machine that deployed it was powered off on holiday — a timer trigger is Azure-side and owes nothing to any developer machine being awake. Also note the `az monitor app-insights query ... -o table` rendering silently prints nothing for some result shapes; a `union * \| summarize count()` sanity check distinguishes "no data" from "no output" and is worth running before concluding a resource is dead. | B12 | done |
| A4 | Android groups client layer (003 §5/§6.1): `GroupsApi` port + DTOs (10 endpoints), 6 new error codes mapped + user messages (27 total) | — | done |
| A5 | Android groups screens (003 §12.2): list (= family-less home) / create (policy privacy copy) / detail + share code / join + `findly://group-join` deep link / group map (position-only), nav additions | A3, A4 | done |
| I4 | iOS groups client layer (004 §3.1–3.2): FindlyKit client methods + DTOs (10 endpoints), 6 new `APIErrorCode` cases (27 total) | — | done |
| I5 | iOS groups screens (004 §3.4): same inventory as A5, `AppRoute` additions, deep-link parsing in FindlyKit | I3, I4 | done |
| B13 | `PLAN_MATRIX` group-member cap 50→200 (001 §9/§10/§12.6, 005 §4; 000 §D15) — matrix values + affected test literals only, no new logic | — | done |
| W1 | Join-link web artifact (007): `web/join/` static landing page (`/g`, fragment-read, no-oracle, no external resources), `.well-known/assetlinks.json` + `apple-app-site-association`, `staticwebapp.config.json` (routes + AASA content-type), SWA deploy workflow (OIDC, run-time-fetched token, `contents: read` + `id-token: write` only) | — (CI deploy verifies after H4) | done |
| A6 | Android public join links (007, 003 §12.3): `JOIN_LINK_HOST` build config, App Links intent filter (`autoVerify`), https fragment parsing via `GroupJoinCodeSanitizer`, share text → https link, on-device QR on the detail screen | A5 | done |
| A7 | Android release signing wiring (docs/store-readiness.md §1): `signingConfig` from CI/env references (no keystore or secret in repo), resolve `android.yml`'s release-build TODO | — | done |
| I6 | iOS public join links (007, 004 §3.5): `AppConfig.joinLinkHost`, `GroupCodeParsing` https+fragment form, https `ShareLink`, CoreImage on-device QR; Associated Domains entitlement **prepared** (activation is H6, enrollment-gated) | I5 | done |
| H4 | Provision the join-link host (azure-setup §7): SWA Free + OIDC deploy RBAC; record `JOIN_LINK_HOST`; after W1 merges, verify `/g` + both `.well-known` files (content type, no redirect) | — | done |
| H5 | Google Play (store-readiness §1): account check (personal-account closed-testing rule), release keystore + Play App Signing (app-signing SHA-256 → Firebase §6.5 + W1 `assetlinks.json`), background-location declaration, data-safety form (needs the live `/delete-account` URL → W2), listing | A7, H7, W2 | human |
| H6 | Apple App Store (store-readiness §2). **NOT blocked — enrollment COMPLETED** (Team ID `92A2K3Q7NH`, verified live in the served AASA 2026-07-25; a Team ID cannot exist without an active membership). Remaining portal tail: activate Associated Domains + Push + App Attest on the `com.findly.ios` App ID, upload the APNs `.p8` to Firebase (unblocks iOS on-device phone sign-in **and** iOS App Check → H8), apply for the O1 Location Push entitlement, create the App Store Connect app + TestFlight, privacy labels. Needs I9 (`.xcodeproj`) before a build can be uploaded. | H7 | human |
| H7 | Legal & naming (store-readiness §0): naming DONE 2026-07-25 (renamed to Findly, see docs/findly-rename-runbook.md); privacy policy + ToS authored → **hosted & verified live at `/privacy` + `/terms`** (PR #2, merged 2026-07-25). Unblocks H5/H6. | H4 | done |
| H8 | Go-open switch (006 §6.3): App Check **enforced** on both platforms → SMS regions open to all → budget alert €25; incident runbook 006 §7. **Before enforcement: the web app (H9) must carry its App Check provider too, or the 008 §6 deletion page's sign-in breaks** | H2, H6, H9 | human |
| B14 | `apiCalls` placement fix (001 §9): move the increment from each domain use-case's success path into the shared `authGuard`/HTTP layer, so every authenticated request counts exactly once (incl. 4xx) — across B1/B2/B3's call sites together, not piecemeal | — | done |
| B15 | Catch-all error-log hardening: every `*.functions.ts` `errorResponse()` catch-all logs only `err.message`/`err.code`, never the raw `err` object | — | done |
| B16 | Clear `pushInvalid` on a fresh token (001 §4.1/§8.5): `registerDevice.ts` re-registration with a new `pushToken` must reset any prior `pushInvalid` flag | — | done |
| I7 | iOS `verificationID` → Keychain (hardening pass, raised in I3's security review): `KeychainStoring` protocol + `InMemoryKeychainStore` fake (FindlyKit), real `Security`-framework `KeychainStore` (app target) swapped into `FirebaseAuthProvider` in place of `UserDefaults` | I3 | done |
| B17 | Export endpoint (001 §13.1, 008 §3): streaming unenveloped JSON download, full-retention read (beyond `historyDays`), parent-for-member authorization, `exportsPerDay` quota (`PLAN_MATRIX` + `exports` usage metric) | — | done |
| B18 | Account deletion (001 §13.2, 008 §4, 002 §4.2): full erasure incl. events filtered rewrite, devices-first/profile-last ordering, crash-retry idempotency, no-profile no-op, last-parent/sole-member cascade into B19's family-delete logic | B19 | done |
| B19 | Family deletion (001 §13.3, 008 §5, 002 §4.2) + invite index rows (002 §2.1, write path in 001 §3.3) + §3.4 fail-closed accept on deleted families; members flip family-less (others first, caller last) | — | done |
| W2 | Web deletion page (008 §6): `/delete-account` on the SWA — Firebase JS phone sign-in, confirmation, `DELETE /users/me` via CORS, Firebase `user.delete()`, failure/retry states; static checks in W1's spirit | B18, H9 | done |
| A8 | Android privacy UI (003 §12.4): settings entries for export / delete account / delete family, two-step confirmations incl. cascade wording, Firebase-delete ordering + retry, full local-state wipe, export share-sheet | — | done |
| I8 | iOS privacy UI (004 §3.6): same inventory, FindlyKit client methods (raw-`Data` export path), `.confirmationDialog` flows, Keychain/local wipe | — | done |
| H9 | Privacy-page infra (008 §6.2): register a Firebase **web app** in `findly-71f7b` + authorized domain (SWA host); CORS on `func-findly` for the SWA origin (`DELETE` + `Authorization`); web App Check provider registered **before** H8 flips enforcement | — | human |
| A9 | Android real FCM (**009 §5**, 003 §9, 001 §8): `FirebaseMessagingService`, real `PushTokenProvider` replacing the stub, handlers for the four §8 push types, token-refresh re-registration | — | done |
| A10 | Android real tracking (**009 §1–§4, §9**, 003 §10, 000 §O2): fused location capture, WorkManager periodic wiring (§10.5 TODO), foreground service for 5/10-min intervals, Room-backed `FixQueueStore` | — | done |
| A11 | Android platform geofencing (**009 §6–§7**, 003 §11, 001 §7): `GeofencingClient` registration from synced config, transitions → §7.3 reports + `source:"geofence"` fixes | A10 | done |
| A12 | Android real map (003 §12): Google Maps SDK behind the `MapRenderer` seam, replacing `PlaceholderMapRenderer` (`MAPS_API_KEY` provisioning is a Phase-0 human step, roadmap) | — | done |
| I9 | iOS `.xcodeproj` app target (004 §1.1, long-standing): real app shell; `ios-build` CI stub becomes a real build; **wire the app icon** — `AppIcon.appiconset` from `design/findly-icon/generated/AppIcon-1024-{light,dark,tinted}.png` (already rasterised, alpha-free) | — | done |
| B20 | **Erasure vs never-created tables/containers (002 §4.2, 008 §9)** — live bug: `DELETE /users/me` returns **500 `TableNotFound`** on `func-findly` because the erasure paths *list* before they delete and the list call doesn't tolerate a table/container that was never created. Only this endpoint is affected (it's the one permitted without a profile — every other endpoint short-circuits on `PROFILE_NOT_FOUND` first). Not fresh-environment-only: a family that never uses locate never creates `LocateRequests`. Fix the list step in every erasure adapter + an Azurite test with the table deliberately absent | — | done |
| B21 | **`clearFamilyMembership` never actually clears anything (002 §2, §4.2)** — live bug, found during B20's audit and independently reproduced against real Azurite: `TableUserRepo.clearFamilyMembership` used `Merge` with `familyId: null, role: null` — Azure Table Storage's Merge has no `null` type, so the call "succeeds" (ETag changes) while the old values are left completely unchanged. Used by B19's family-deletion footprint and B18's account-deletion cascade to flip surviving members family-less — meaning **every family/account deletion ever run has left stale `familyId`/`role`** on affected profiles. Real-world exposure is low (pre-launch, test data only, an account reset is already planned), but the guarantee itself was never real. Fixed: read → ETag-guarded `Replace` preserving `displayName` → bounded retry on conflict → throw (not drop) on exhaustion, since this is a correctness guarantee, not telemetry. No spec change needed for the *required behavior* (already correctly specified); 002 §2 gained the general Merge-cannot-clear-a-field rule so this class of bug isn't rediscovered per-field | — | done |
| B22 | **Sibling of B20 in the locate flow (001 §6.1, 002 §2.7, §2 general list-tolerance rule)** — live bug, found during B20's review (the implementing agent found it too but never wrote it down anywhere — closing that gap here). `createLocateRequest.ts`'s coalescing check calls `locateRequestsTableRepo.listPendingByTargetDevice` **before** the first `LocateRequests` row is ever created for that family — so a family's **very first** push-to-locate request 500s with `TableNotFound`, identical failure mode to B20, just on a different endpoint. Fix: route `listPendingByTargetDevice` through the same `collectEntitiesTolerant` helper B20 already built; no new helper needed, no spec change beyond the general rule already added for B20/B21. Small, well-scoped, reuses existing infrastructure | — | done |
| A14 | **Bump Android `compileSdk`/`targetSdk` to 37** — recommended by A12's code review: the Homebrew Android SDK here only has platform 36 installed, which forced A12 to pin `maps-compose` to an older release (8.0.0+ requires compileSdk 37). Not urgent on its own, but Play Store's annual target-API mandate will force this regardless of any Maps SDK decision — worth doing as its own standalone task rather than absorbing into a dependency pin | — | done |
| B23 | **Self-healing table creation, defense-in-depth (002 §1)** — the actual root cause behind B20/B22 (and the live incident logged 2026-07-25): `stfindly` had zero Table Storage tables because nothing anywhere ever creates one, and Azure Table Storage does **not** auto-create a table on first write (unlike blob containers, which already self-heal via create-if-not-exists on append, 002 §3.2). Fixed live via manual `az storage table create` for all 13 tables + both provisioning scripts corrected — but that's ops-level, not code-level: a future infra rebuild that misses the (now-documented) provisioning step would silently repeat this exact incident. Make `createTableClient`/the table-adapter layer idempotently ensure its table exists (matching the blob store's existing idiom) so a missing table self-heals instead of 500ing. TDD + full review gate | — | done |
| B24 | **`SETTINGS_CHANGED` push never sent (001 §8.3)** — `fcmV1Sender.ts`'s `buildFcmBody` had no case for `SETTINGS_CHANGED` at all (marked out-of-scope by B4/B5's own header comment), so every call from `patchDeviceSettings.ts` threw and was silently swallowed by its best-effort try/catch. Found by I12's iOS push work. Not user-visibly broken — delivery was always best-effort, with the `POST /locations` piggyback and paused-device poll as the two guaranteed pickup paths every client already relies on — but a real, silent gap since B4/B5. Added `buildSettingsChangedBody` matching §8.4's identical envelope shape, verified against the spec text directly | — | done |
| A13 | Android app icon + notification icons (`design/findly-icon/`, 009 §8): drop the adaptive-icon `VectorDrawable`s into `res/mipmap-anydpi-v26/` + `res/drawable/`, wire `ic_stat_findly`/`ic_stat_locating`, splash drops the halo. Assets are delivered and final — integration only, no design work | — | done |
| I10 | iOS real location (**009 §1–§4, §9**, 004 §6–§7, 000 §O2): `CLLocationManager` conformance to `LocationProviding`, background delivery, persistent fix store | I9 | done |
| I11 | iOS region monitoring (**009 §6–§7**, 004 §7, 000 §O9): 20-region registration + transition reporting | I10 | done |
| I12 | iOS push registration + handling (**009 §5**, 004 §7, 001 §8): FCM-routed APNs; Location Push plumbing dormant until O1 | I9 | done |
| I13 | **`FindlyKit` `swift test` intermittently crashes (SIGABRT, "freed pointer was not the last allocation")** — found during I9's build verification (the first time this suite ran on a real Xcode host instead of the out-of-tree headless harness), reproduced 4/4 runs. Not a regression (`FindlyKit/` has a zero-line diff in I9). Prime suspects: shared `private static let`/`static var` `DateFormatter`/`ISO8601DateFormatter` instances in `Screens/Groups/GroupModels.swift`, `CreateGroupViewModel.swift`, `GroupDetailViewModel.swift`, `Screens/History/HistoryViewModel.swift`, `HistoryScreen.swift` — classically unsafe under Swift Testing's concurrent suite execution even as a `let`. `MockURLProtocol.requestHandler` was checked and ruled out. Make each formatter instance-local or wrap in a synchronized accessor | — | done |
| I14 | **`DeleteAccountViewModelTests.confirmDelete_success_...` is unsatisfiable by construction** — found during I11's review, confirmed pre-existing (reproduces identically on the pre-I11 commit) and unrelated to any of tonight's work. The test's own fake, `InMemoryDeviceIdProvider(generateUUID: { "dev-1" })`, always returns the fixed string `"dev-1"`, but the test then asserts the regenerated device id `!= "dev-1"` ("must be a fresh id") — impossible given the fake's own configuration. Fix the fake to generate a genuinely varying id (or assert equality with the *expected* fresh value instead of inequality with the stale one) | — | done |
| I15 | **iOS `GEOFENCE_EVENT` client-side notification rendering is unreachable dead code under the current push wire shape** — found during I12's review, verified empirically via real `xcrun simctl push` injection + OS log inspection. 001 §8.2's shape (`aps.alert.title` set, `mutable-content:1`, no `content-available`) means iOS auto-displays the server-embedded title before the app-level `PushMessageDispatcher` ever runs, for both foreground and background delivery — `GeofenceEventPushHandler`/`GeofenceEventNotificationTemplate`/`SystemGeofenceEventNotifier` are correct and tested but never actually execute in the shipped app. Not user-visibly broken (server and client build identical title text off the same template); `mobile/ios/README.md` corrected to describe actual behavior rather than the previous false "deliberately not relying on the OS" claim. The real fix is a `UNNotificationServiceExtension` app-extension target (re-render from `data` before display, per 000 §O8) — a genuinely separate, larger task, not scoped into I12 | — | todo |
| A15 | **`:app:lintVitalRelease` fails — will break the first signed `main` build the moment H5 lands** — found during A14, and confirmed **pre-existing** (not an A14 regression) by the code reviewer independently checking out base commit `b00eb9b` into a separate worktree and reproducing the identical failure at compileSdk 36. Two lint errors: `InvalidFragmentVersionForActivityResult` (`MainActivity.kt:62`) and `RemoveWorkManagerInitializer` (the WorkManager / `androidx.startup` `InitializationProvider` conflict). Currently dormant in CI purely because the `assembleRelease` step is gated on `ANDROID_KEYSTORE_BASE64 != ''` **and** `main` — H5 provisioning the real signing secrets is exactly what un-dormants it, so this must be fixed **before** H5, not after. Note the failing task is `:app:lintVitalRelease`; `lintVitalReportRelease` is a related report-generation task whose output path merely appears in the error text | — | todo |
| A16 | **Unpin `maps-compose` 7.0.0 → 8.x now that compileSdk is 37 (A14)** — A12 pinned 7.0.0 on the belief that 8.0.0+ required compileSdk 37 via `androidx.core:core-ktx 1.19.0`. That belief was **wrong on both counts**, established during A14's review from primary sources: `maps-compose-8.0.0`'s POM resolves `core-ktx:1.17.0`, and that AAR's own `aar-metadata.properties` declares `minCompileSdk=36` — so 8.0.0 was never actually blocked by compileSdk at all. A14's coding agent empirically built and tested 8.0.0 at compileSdk 37 (clean `assembleDebug` + full suite green), then correctly reverted it as out of scope rather than smuggling a dependency bump into a config diff. This task lands it properly on its own reviewed diff: verify the resolved graph with `gradle :app:dependencies` (A12's own standard, not POM-reading alone), decide the exact pin, and correct the residual comment vestige in `app/build.gradle.kts` that still cites compileSdk-36-compatibility as if it distinguished 7.0.0 from 8.0.0 — under the corrected facts it does not | A14 | todo |

**Mobile H1-waiver (user directive, 2026-07-19):** the mobile clients code against **specs/001 (complete, normative)**, not the backend implementation, so they run now with H1 (Azure/Firebase provisioning) still `human`/pending. All H1-dependent runtime bits — real `google-services.json`/`GoogleService-Info.plist`, live backend base URL, real Firebase project — are **stubbed behind interfaces and gitignored**; wire them for real when H1 is done. No secrets committed. **Final deliverable (do LAST, after coding is maximized):** a design-generation prompt (`docs/design-prompt.md`) targeting each app's documented design-token contract, so a design tool can produce the iOS + Android visual design that drops into the swappable layer.

**Working the backlog:** invoke **`/dev-loop`** (skill in `.claude/skills/dev-loop/`) — e.g. `/dev-loop parallel 3` runs up to 3 dependency-safe coding agents concurrently in isolated worktrees; every task passes a **code review + security review gate** ([docs/security-review-checklist.md](security-review-checklist.md)) before it may merge. Manual sessions implementing a task directly MUST run the same gate before committing to main.

## Dev-loop log

*(appended by /dev-loop — newest first: date · task · agent rounds · review findings fixed · merge commit)*

- **2026-08-02 · A14** · 2 rounds · reviews: security **approved outright, zero findings, both rounds**; code review round 1 returned **1 Blocking + 1 Major**, of which the Blocking was **adjudicated invalid by the orchestrator and formally retracted by the reviewer**. `compileSdk`/`targetSdk` 36 → 37, `minSdk` unchanged at 26. The environment premise recorded in A14's own row had gone stale: only `platforms;android-36` was installed, but API 37 *is* installable — with the wrinkle that **there is no plain `platforms;android-37` package**, only `android-37.0`/`37.1`/`37.2-beta1`, because API 37 uses Android's minor-SDK-version scheme. Verified empirically that plain `compileSdk = 37` is nonetheless sufficient (AGP resolves it to hash string `android-37.0`; no `compileSdkMinor` property needed), and that the merged manifest really emits `targetSdkVersion="37"` — not assumed from the DSL. Red evidence was honest for a config-only task: with the platform uninstalled and `-Pandroid.builder.sdkDownload=false`, the build fails `Failed to find target with hash string 'android-37.0'`; auto-download stayed disabled for every verification run so no green result could be hiding an implicit network fetch. **The Blocking is worth recording in full, because the failure mode was a reviewer being confidently wrong in a checkable way.** It claimed the new CI step's `yes | sdkmanager --licenses` line would abort under `-o pipefail` before the install line ever ran, breaking `android-build` on every run — and backed it with a reproduction. The reproduction ran `set -eo pipefail` explicitly, which demonstrated the mechanism correctly while *assuming the premise under test*. GitHub's default shell for a `run:` step is `bash -e {0}` **without** pipefail; the `--noprofile --norc -eo pipefail {0}` form applies only when `shell:` is explicitly specified, and this repo sets no `shell:` key anywhere and no `defaults.run` in `android.yml`. On being shown this the reviewer went to the `actions/runner` source itself rather than docs prose and found the precise mechanism: the default path resolves the **bash binary** (preferred over sh) but looks up **`sh`'s argument format**, `-e {0}` — pipefail only ever enters via `ParseShellOptionString` on an explicit `shell:`. It then re-ran its repro under the real default and got a clean exit, and withdrew the finding. The `|| true` guard was kept anyway as deliberate hardening against a future `shell: bash` addition, with the comment **and** commit message explicitly stating it is not a fix for a live break — the tree should carry an accurate record, not a dramatic one. The Major was real and independently sourced twice (by the coding agent, then by the reviewer from the `maps-compose-8.0.0` POM and `core-ktx`'s `aar-metadata.properties`): the "corrected" maps-compose comment fixed its stale *compileSdk 36* framing while leaving intact a neighbouring claim the same investigation had disproved — `core-ktx 1.19.0` demanding compileSdk 37, when the real numbers are `1.17.0` and `minCompileSdk=36`. Rewritten to state only verified facts. One residual comment vestige (still citing compileSdk-36-compatibility as though it distinguished 7.0.0 from 8.0.0, which under the corrected facts it does not) was **logged rather than fixed** — a deliberate call not to spend the second of two allowed fix rounds on a comment nit while a real finding might still need it; folded into **A16** instead. CI gained an explicit `sdkmanager` install step: the reviewer notes `ubuntu-latest` likely already ships platform 37, making it near-no-op today, but the implicit AGP auto-download path is silently network- and license-dependent and the runner image's SDK refreshes on its own cadence — keeping it deterministic, given this repo's 2026-07-28 local-green/CI-red history. Two follow-ups filed from the task's own findings: **A15** (pre-existing `:app:lintVitalRelease` failure, independently reproduced at base commit `b00eb9b`, which will break the first signed `main` build the moment H5 provisions signing secrets) and **A16** (maps-compose 8.x, empirically green at compileSdk 37 and correctly reverted rather than smuggled into a config diff). Result: 491/491 Android unit tests + clean `assembleDebug` on merged `main`; backend gate re-run post-merge (unaffected, Android-only): 704 tests, `tsc` clean, mutation 99.17% vs break 60 · merge `d277487`. **Pushed and confirmed green on the real runner** (`android` run `30751033549`, commit `3f5398b`, `android-build` ✓ 2m16s) rather than asserted from local runs — the 2026-07-28 lesson applied deliberately, and doubly warranted here because the CI step itself was the new code. The runner log closes the pipefail dispute empirically and permanently: GitHub printed `shell: /usr/bin/bash -e {0}` for the step — no `-o pipefail` — and the log even shows `yes: standard output: Broken pipe` followed by the install proceeding to completion, i.e. the exact SIGPIPE the Blocking predicted would abort the step, occurring and being harmless. Future sessions: **the `shell:` line GitHub prints in every step's log group is the authoritative answer to "which bash flags am I running under"** — cheaper and more reliable than reasoning from docs prose.

- **2026-07-28 · post-merge CI sweep (orchestrator, not a backlog task)** — after pushing I13's merge, ran a full CI/CD circle per user request rather than assuming green. Found `ios` had actually been **red on `main` since before I13** — reproduced identically on the prior commit (`c688648`), so not caused by I13/I14. Two independent, pre-existing causes, both confirmed via GitHub's raw job logs (not just the summary UI): (1) `ios-build`'s `xcodebuild` destination pinned `iPhone 15` + `OS:latest`, but the runner image's newest simulator runtime (18.2) only pairs with iPhone 16+ (`iPhone 15` tops out at 17.5) — `Unable to find a device matching the provided destination specifier`; fixed by switching to `generic/platform=iOS Simulator` (this job only builds, never runs, so it never needed a resolved device at all). (2) `ios-package`'s `LocationRuntimeContainerTests` compared `scheduleCalls` (`[TimeInterval?]`) against array literals containing the *expression* `6 * 60 * 60` — apparently compiler-version-dependent whether that unifies with `Double?` (local Xcode 26.6 accepted it, CI's Xcode 16.2 didn't: "cannot convert value of type 'Int' to expected element type ... Optional<Double>"); wrapped in `TimeInterval(...)` to remove the ambiguity outright. Both verified locally before pushing (`swift build --build-tests` + `swift test --filter LocationRuntimeContainerTests`, 20/20; `xcodebuild build -scheme Findly -destination 'generic/platform=iOS Simulator'` → `BUILD SUCCEEDED`), then confirmed green on GitHub for real (`ios #28`, commit `a04918c`) alongside the other three workflows' already-green latest runs (`android #30`, `backend #31`, `web-join #10`) — all four green on `main`'s tip. Lesson for future sessions: this repo's iOS task log entries have been asserting "build clean" from local `swift build`/`xcodebuild` runs without independently confirming the GitHub-hosted CI actually went green — the two are not the same claim, and they'd silently diverged here.

- **2026-07-28 · I13 (+ I14 reconciled)** · 2 rounds · reviews: code review **approved** (independently re-ran `swift test` 4x, verified diff scope and the `Duration`→nanoseconds conversion math, confirmed commit hygiene), security review **approved** (2 Low, non-blocking, informational notes on unreachable edge cases — trap-on-overflow/zero-sleep-on-negative — in the new sleep-conversion helper, currently unreachable from any real call site). The handoff's own DateFormatter-sharing hypothesis was **wrong, not force-fit**: bisected out via `swift test --skip <suite>` across all five suspected Groups/History suites (39+ tests), crash persisted with zero of them running. Real cause found via symbolicated macOS crash reports (`swiftpm-testing-helper-*.ips`, independently re-inspected by the code reviewer): `SignInViewModel.beginCooldown(phone:)`'s `await self.sleep(.seconds(1))` hit a known Swift concurrency runtime defect (swiftlang/swift#86204) — `Task.sleep(for: Duration)` specialized into both the `FindlyKit` and `FindlyKitTests` binary modules corrupts the task allocator on deinit. Fixed by swapping the default `sleep` closure to a `Task.sleep(nanoseconds:)`-based equivalent in `SignInViewModel.swift` (isolated repro: 2/2 crashed before, 3/3 clean after) and defensively in `LocateViewModel.swift` (identical `[weak self]` polling shape, same module — its own tests just never happened to race it). Verified with 10+ full-suite runs post-fix, zero crashes, 565/565 passing. **Orchestrator caught one false regression alarm mid-review**: diffing the coding agent's branch against a moving `main` (which had independently gained an unrelated commit mid-session) made an untouched file look modified; re-diffing against the true fork point cleared it — lesson: diff against merge-base, not a live branch ref, when other work can land on `main` concurrently. That same commit, `c688648`, turned out to **independently and completely fix I14** (the unsatisfiable `DeleteAccountViewModelTests` device-id assertion — `generateUUID` was a constant `"dev-1"` closure) with exactly the fix I14 specifies; it had landed on `main` outside the dev-loop process with no ledger update, reconciled to `done` here. I13's own spawned follow-up task for that same bug (`task_b5a2a3d8`) was correctly identified as stale once this was found, and dismissed. Result: `FindlyKit` full suite green and crash-free (565 tests), `swift build --build-tests` clean, backend gate re-run post-merge (unaffected, iOS-only): 704 tests · merge `6dc59b3`.

- **2026-07-28 · I12 — device-runtime wave complete (Android A9–A13, iOS I9–I12, both platforms).** 2 rounds · reviews: security **approved outright** (including a careful check that the real local Firebase key used for on-machine verification never leaked into anything tracked); code review found **1 Blocking-at-merge-time + 1 Major**. Real Firebase Messaging SDK integration (`firebase-ios-sdk` 11.15.0, `FirebaseMessaging`+`FirebaseCore` only, `Package.resolved` pinned) bridging APNs device tokens to FCM registration tokens — the token kind the backend's FCM HTTP v1 sender actually expects, matching Android's model — plus all four push-type handlers (`LOCATE_REQUEST` bypasses `FixCaptureCoordinator` directly so a paused device still fulfills; `SETTINGS_CHANGED` full-state via `DeviceSettingsApplying`; `GEOFENCE_EVENT`; `GEOFENCE_CONFIG_CHANGED`). Code review's Major finding, verified empirically via real push injection + OS log inspection: `GEOFENCE_EVENT`'s client-side notification path never actually runs under the current wire shape (iOS auto-displays the server's own embedded title first) — not user-visibly broken, but the README's claim about it was wrong; corrected, and the real fix (a `UNNotificationServiceExtension` target) tracked separately as **I15**. The Blocking finding was a direct consequence of running I11 and I12 in parallel: both independently built a full `GeofenceConfigSyncCoordinator` implementation for the same seam, a guaranteed compile collision plus real conflicts in three shared composition-root files. Resolved once I11 (the real implementation) merged: I12's placeholder trio deleted entirely, its push handler rewired to share I11's one real coordinator instance — the same one every other geofence registration trigger already drains through — rather than a second, independently-constructed one that could disagree with it. Reconciliation independently re-verified, including confirming I11's three-round sign-out-wipe fix (see below) survived the merge with its exact step order intact. Result: 169/169 relevant tests, re-verified twice independently on the merged `main` · merge `79041fd`.

  **Wave summary:** every device-runtime task (A9 real FCM, A10 real tracking, A11 platform geofencing, A12 real map, A13 app icon, I9 Xcode project, I10 real location, I11 region monitoring, I12 push) is now merged and independently reviewed on both platforms, plus two bugs found along the way and fixed in their own gated tasks (A9-fix: first-sign-in push token drop; B24: `SETTINGS_CHANGED` push never sent). Both apps can now capture real location, queue it durably through process death, sync it, register and react to geofences, receive push, and render it on a real map. Three small, non-blocking follow-ups tracked for later: **I13** (pre-existing `FindlyKit` test flake), **I14** (pre-existing broken test assertion), **I15** (`GEOFENCE_EVENT`'s Notification Service Extension). Remaining before store submission: H5/H6 (real signing/store listings), H8 (go-open switch), and the App Store/Play Store human-side tasks already tracked above.

- **2026-07-28 · I11** · 3 rounds · reviews: security **not approved as-is (1 High)**, code review found the same root cause independently **(1 Major)** plus 1 Minor. Real `CLLocationManager` region monitoring (`SystemGeofenceRegistrar`), a local geofence-config cache, and a SQLite-backed durable geofence-event queue matching I10's `SQLiteFixStore` discipline exactly — no repeat of I10's one string-interpolation slip, confirmed by both reviewers. All five spec-required re-registration triggers wired (sign-in, both piggyback ETag paths, resume, cold start). Round 1 found: no local state was wiped on sign-out — only on account deletion — so a signed-out family's still-registered geofences kept firing real transitions that got durably queued and reported under whichever family signed in next on the same device, tagged with the wrong family's `geofenceId`s. This was a pre-existing gap from I10 (never flagged, since I10's own review was scoped to pause/resume, not sign-out) that I11 widened to cross-family-sensitive data — the security reviewer's own framing: "arguably more sensitive than raw coordinates... human-labeled." Round 2 built one consolidated `LocationRuntimeContainer.wipeLocalState()` covering everything I10+I11 persist, wired into all three sign-out paths (forced sign-out, the spec-mandated sign-out-for-retry recovery flow, and account deletion — replacing its previous partial ad-hoc wipe so there's one implementation instead of two diverging ones). Round 3, caught by a dedicated re-verification pass: the pause-state write that actually makes the wipe's "drop, don't queue" guarantee work was running *last*, after two SQLite-backed suspension points a concurrently-racing transition (delivered via an unstructured `Task`, decoupled from the container's own execution order) could get past while still reading stale state — narrowing the original leak from "always happens" to "happens if a transition races the wipe's I/O window," not negligible right around a forced sign-out. Fixed by making the pause-state write the synchronous first step. The regression test proving this races a transition against the wipe for real (not sequentially, which the first version of the test didn't catch) — independently reproduced **15/15 pass with the fix, 15/15 fail without it, zero flakiness either direction**. Result: 94/94 geofence tests, re-verified three times independently on the merged `main` · merge `455625d`.

- **2026-07-28 · I10** · 2 rounds · reviews: security **approved with 1 Low finding** (fixed), code review round 1 came back with **1 Blocking + 3 Major + 3 Minor**. The iOS analog of Android's A10, and the largest iOS task of the wave: real `CLLocationManager` capture (staged When-In-Use → Always authorization), the three opportunistic triggers (`BGAppRefreshTask`, significant-location-change monitoring, foreground), and a SQLite-backed `FixStoring` replacing the in-memory placeholder — the first raw-SQL code in the codebase (chosen over Core Data specifically to avoid hand-editing a `.xcdatamodeld` with no Xcode GUI model editor available, same reasoning as I9's `xcodegen` choice). `FixQueue`'s own state machine (already correct, freeze-on-first-send/`batchId` reuse) was deliberately left untouched — only `FixStoring` was widened so the in-flight `PendingBatch`'s identity is itself durable (`freezeNextBatch`/`markAccepted`/`markRejected`/the 1000-cap eviction each one atomic transaction), closing the same class of gap A10 closed on Android. Round 1 found: **no background trigger survived pause** — `scheduler.cancelAll()` on the pause path cancelled the *only* thing 009 §4 says must keep running while paused, silently defeating the "resume within 6 hours even if the app is never reopened" guarantee; pause didn't stop significant-location-change monitoring; the sync runner attempted a flush while paused instead of gating client-side; and — the most architecturally significant finding — `RootView.init()` was reconstructing the entire location stack (SQLite connection, `FixQueue` actor, live `CLLocationManager`) on every in-app navigation rather than once per launch, proven not by inspection but by the reviewer writing a standalone SwiftUI reproduction of the exact `@StateObject`/`@ObservedObject` pattern and observing the reconstruction empirically. Round 2 fixed all seven findings — the BG task now stays scheduled through pause and polls for resume on a ≤6h bound instead of being cancelled; pause/resume drive location monitoring symmetrically; the sync coordinator gates on a live settings closure before touching the queue; the SQLite-backed stack's construction moved into `FindlyApp.init()` (SwiftUI-guaranteed once-per-launch) and is passed into `RootView` as a parameter. Every fix independently re-verified, including attaching `lldb` to a real simulator build and confirming the composition-root fix by observed breakpoint hit counts (exactly 1, not just "the code looks right"), and `xattr`-inspecting the actual on-disk database file to confirm a non-blocking backup-exclusion suggestion was genuinely applied. Result: 424 tests (up from ~330 pre-task), build clean, re-verified twice independently on the merged `main`. Surfaced but correctly left as a documented, non-blocking follow-up (both reviewers agreed): the first sync after a fresh sign-in always 404s once before `DeviceRegistrationService` self-heals — an eager post-sign-in registration call would avoid it · merge `ef11743`.

- **2026-07-27 · I9** · 1 round · reviews: both **approved outright, zero findings**. The long-standing iOS blocker: `specs/004-ios-client.md` §1.1 explicitly deferred creating `Findly.xcodeproj` until a session had real Xcode to validate the result ("hand-authoring a `project.pbxproj`... is worse than no project file at all"). That session was tonight — Xcode 26.6 + a licensed install landed earlier in the wave. No GUI automation is available to drive Xcode's own File→New→Project wizard, so the project was generated from a declarative `project.yml` via `xcodegen` instead (`brew install xcodegen`) — reproducible and diffable, then validated for real rather than trusted: `xcodebuild build` for a generic Simulator destination succeeded, and a concrete iPhone 17 simulator build/install/launch was screenshotted showing the real sign-in screen and app icon rendering. iOS 16 deployment target (matching `FindlyKit`'s `Package.swift`), bundle id `com.findly.ios`, `FindlyKit` wired in as a local Swift Package dependency, shared (not user-scoped) scheme. Zero business logic added to the app target — wraps the existing, already-reviewed `Findly/` sources unchanged (five files byte-identical pre/post-diff, independently confirmed by both reviewers) plus the app icon (byte-identical to the already-committed `design/findly-icon/generated/` PNGs). `FindlyKit/` itself has a zero-line diff — the SPM-package boundary the whole split exists to protect is intact, independently confirmed by both reviewers rather than assumed. `ios-build` CI is now a genuine `xcodebuild` job (was a structure-check stub), with a placeholder `GoogleService-Info.plist` step mirroring Android's existing pattern — security review specifically hunted for the real, gitignored local Firebase key (used for this session's own on-machine verification) leaking into anything tracked, and found nothing. Surfaced, not fixed (out of scope, tracked as **I13** above): a pre-existing `FindlyKit` `swift test` flake, reproduced 4/4 by the code reviewer independently, provably unrelated to this diff. Unblocks I10/I11/I12. Environment note: the iOS Simulator runtime download that made the live simulator verification possible needed two attempts on this machine — the first attempt's install step collided with stale disk-image state (most likely residue from the session's earlier kernel panics interrupting an in-progress download), cleared by a second clean download · merge `68d5223`.

- **2026-07-27 · A11** · 2 rounds · reviews: security **approved with 1 low finding**, code **approved with 1 Major finding (recommended, not blocking)**. The last piece of the Android device-runtime wave: `GeofencingClientManager` — one real class implementing both `GeofenceRegistry.unregisterAll()` (A10's pause seam) and `GeofenceRegistrar.registerAll(geofences, etag)` (A9's config-changed-push seam), exactly as A10's report predicted — plus a local geofence-config cache (doc + ETag, previously missing) and a Room-backed durable geofence-event queue matching A10's `FixQueueStore` pattern exactly. All five spec-required re-registration triggers wired: first sync after sign-in, `geofenceEtag` mismatch on both the `POST /locations` and `POST /geofence-events` piggybacks, the `GEOFENCE_CONFIG_CHANGED` push, resume from pause, and app cold start (covering reboot/reinstall, since both lose OS-level geofence registrations with no callback to hook). Transition handling durably queues one event plus a `source:"geofence"` fix per callback via a `goAsync()`-based `BroadcastReceiver` — code review independently verified the `PendingResult` is genuinely `finish()`ed on both the success and exception path (a `finally` block), the direct analog of A10's PendingBatch-identity scrutiny applied to a new durability primitive. Code review found one Major, not-blocking-but-recommended finding: the Room DB's v1→v2 bump used `fallbackToDestructiveMigration`, which would wipe A10's entire unsent fix queue on any schema upgrade, not just add the new table — the reviewer independently verified via `docs/store-release-roadmap.md` that no real distribution channel exists yet (so tonight's actual risk was genuinely zero) but recommended fixing it anyway since the correct fix was cheap and this project has already been burned once by an assumption like this going stale silently (see B21 below). Security review separately found the account-deletion wipe chain cleared the geofence config cache — the most sensitive of the new local stores — last, with no isolation between its 5 steps, so an early failure could leave it behind after an apparently-successful deletion. Both fixed in one round: a real `Room.Migration(1, 2)` (verified via a genuine host-JVM SQLite migration test, `androidx.sqlite:sqlite-bundled-jvm` — the reviewer independently mutated the migration's target table name and confirmed the test actually fails, not just re-read the diff) that leaves `fixes` untouched; each of the wipe chain's 5 steps now independently `runCatching`-isolated (reviewer independently collapsed them back into one shared catch and confirmed the isolation test then fails as expected). Result: 491 tests, build clean, re-verified via direct mutation testing on the merged `main`, not just re-reading.

- **2026-07-27 · A10** · 2 rounds · reviews: security **approved outright** (including a dedicated pass on the PendingBatch-identity and Room-transaction-testability properties — independently decompiled the KSP-generated `FixQueueDao_Impl` and traced the transaction bytecode rather than trusting the doc comments); code review round 1 came back **Blocking** — the mandatory `POST /locations` success-response `deviceSettings`/`geofenceEtag` piggyback (001 §5.1, 009 §1/§3.5) was only applied on the `403 TRACKING_PAUSED` path, so an actively-tracking (non-paused) device whose `syncIntervalMinutes` changed server-side would miss it entirely if the `SETTINGS_CHANGED` push was dropped (routine — Doze, OEM app-killers; push is explicitly best-effort, never the resume mechanism), silently exceeding the spec's one-sync-cycle staleness bound — plus two Minor findings (foreground-service notification opened generic `MainActivity` instead of the device-settings screen; a dead-but-spec-wrong accuracy mapping for `FixSource.Locate` in unreachable code). The largest task of the wave: real `FusedLocationProviderClient` capture, a Room-backed durable `FixQueueStore` (`batchId` lives on the fix row itself, assigned atomically in one `@Transaction`, zero in-memory state — proven by a simulated-process-death test, independently reproduced by the reviewer), WorkManager/foreground-service scheduling selection per 009 §3, pull-based pause/resume, exponential backoff capped at the sync interval. Discovered mid-task that A9 had already landed `LocationCapturer`/`ScheduleRebuilder` seams and adapted to reuse them rather than shipping competing interfaces, extending additively (a default `timeoutMillis` param) so A9's existing `LocateRequestPushHandler` call site was untouched. Round 2 fixed all three findings — piggyback now threaded end-to-end (`SyncOutcome.Synced` carries `deviceSettings`/`geofenceEtag` → `SyncOutcomeReactor` → `LocationSyncRunner` calls `DeviceSettingsCoordinator.applySettings` on every successful sync, keeps draining the queue unlike the paused case) — independently re-verified, including a security-lens look at the new notification-tap intent extra (assessed as a non-issue: `MainActivity` was already exported pre-fix for legitimate deep links, and Settings shows only the signed-in user's own already-reachable family data). Exposes `FixCaptureCoordinator.captureAndQueue(FixSource.Geofence, hint=...)` and `GeofenceRegistry.unregisterAll()` as the seams **A11** needs — A11 will also need to reconcile these with A9's separate `GeofenceRegistrar.registerAll(geofences, etag)`, likely one real class implementing both. Result: 424 tests, build clean, re-verified twice independently on the merged `main`.

- **2026-07-27 · A9-fix** · 1 round · reviews: security **approved outright**, code **approved** (1 Minor, non-blocking — the agent's own RED-proof error count was imprecise, 12 actual vs. 8 claimed; the failure itself was genuine and correctly demonstrated the fix was necessary). Found by A9's own post-merge code review, tracked separately, fixed same night: `HomeStateHolder`'s `SignedIn` handler called `deviceRegistrar.registerOrUpdate(uid = ...)` with no `pushToken` at all — no `PushTokenProvider` was even injected — so a token generated during OTP entry was silently dropped on first-ever sign-in, leaving the device unreachable by push until an unrelated future token rotation (000 §O4, 001 §4.1). Fix: inject `PushTokenProvider` into `HomeStateHolder`, call `currentToken()` on `SignedIn`, thread the result into `registerOrUpdate` — wired through the real construction chain (`MainActivity` → `HomeViewModelFactory` → `HomeViewModel` → `HomeStateHolder`), confirmed to reuse the one shared `AppContainer.pushTokenProvider` singleton, not a second instance. `RealPushTokenProvider.currentToken()` never throws (catches everything but `CancellationException`, returns null), and a null token is omitted from the wire DTO rather than clearing a previously-stored one. Mutation-verified: forcing `pushToken = null` or force-unwrapping `currentToken()!!` each broke exactly the tests meant to catch them. Result: 344 tests, build clean · merge (this session, no separate commit hash captured pre-merge).

- **2026-07-27 · A12** · reviews: code review caught a dependency-version issue and sent it back for correction (1 round) — `com.google.maps.android:maps-compose` shipped pinned to `6.4.4` (the oldest release the agent tried) alongside an explicit `com.google.android.gms:play-services-maps:20.0.0` override that didn't match what `6.4.4`'s own transitive deps actually declared (19.0.0) — an untested pairing. Corrected to `maps-compose:7.0.0` (newest release still safe under this project's `compileSdk 36` ceiling — 8.0.0+ requires compileSdk 37, tracked separately as **A14**) with no explicit `play-services-maps` override, letting it resolve transitively (19.2.0 via `maps-ktx:5.2.1`), verified against the real resolved graph via `gradle :app:dependencies --configuration debugCompileClasspath` rather than assumed. The real Google Maps Compose SDK now sits behind the existing `MapRenderer` seam (`GoogleMapRenderer` replaces `PlaceholderMapRenderer` at `AppContainer`'s composition root; the placeholder stays for previews/tests) — three marker states (online/stale/no-location) via pure, unit-tested `markerStateFor`/`MapCamera`, only the `CameraUpdateFactory` translation touches the real SDK. No emulator in this environment — `assembleDebug`/compile success was the only available verification, no on-device/visual confirmation · merge `dad9ab2`/`86e9cb2`.

- **2026-07-27 · A9** · reviews: code review caught a process violation and sent it back for correction (1 round) — the agent committed the generated Gradle wrapper (`gradlew`/`gradle-wrapper.jar`), against this repo's explicit convention (README + CI both generate it fresh every time, never commit it). Reverted via `git rm --cached` + restored `gradle-wrapper.properties`; the agent went further than the correction asked and added explicit `.gitignore` entries for these paths by name (they weren't covered before), closing the root cause instead of just the symptom (`ef87cee`). Real FCM (009 §5): `FindlyMessagingService`/`RealPushTokenProvider` replace the A1 stub; four push-type handlers (`LOCATE_REQUEST`, `SETTINGS_CHANGED`, `GEOFENCE_EVENT`, `GEOFENCE_CONFIG_CHANGED`) route through a new `PushMessageDispatcher`; a real notification channel for geofence alerts. A separate post-merge code review (not part of the gate — a deeper pass specifically requested given this task's centrality) found the first-sign-in push-token bug tracked and fixed as **A9-fix** above. During this merge's conflict resolution, also wired the real `ic_stat_findly` monochrome icon into `GeofenceEventNotifier` (replacing a placeholder + stale `TODO(A13)`, now that A13's asset had landed) · merge `422bbb4` (icon follow-up `e2131b2`).

- **2026-07-25 · A13** · Android app icon + notification icon integration (`design/findly-icon/`, 009 §8): adaptive launcher icon (`res/mipmap-anydpi-v26/` + `res/drawable/`) and monochrome status-bar icons (`ic_stat_findly`, `ic_stat_locating`), splash drops the halo. Assets were delivered final — integration only, no design work. A follow-up doc pass corrected the icon README's spec citations, which had referenced sections (specs/003 §4.4, specs/004 §2.5) that don't actually exist in either client spec (`dff78cb`) · merge `d854a30`.

- **2026-07-25 · B23** · 1 round · reviews: both **approved outright** (2 Minor, non-blocking notes added as code comments, no re-review needed). The defense-in-depth follow-up to the zero-tables incident logged above: `createTableClient` now wraps every `TableClient` in a `Proxy` that idempotently ensures the table exists (the SDK's own `createTable()`, which swallows only 409 `TableAlreadyExists` — verified against the installed package's compiled source, not assumed) before that table's first operation per process, caching the confirmation so every later call pays a plain `Set.has()` with no round trip. Applied once at the single chokepoint all 13 table adapters already share — none of them needed to change. This was the highest-scrutiny review of the night given the blast radius (every table operation in the whole system): the code reviewer read the SDK source directly to rule out the classic Proxy-wrapping-a-class-with-private-fields failure mode (confirmed `TableClient` uses only plain public fields for this pinned version — flagged as version-contingent, not a permanent guarantee, now a code comment), wrote five throwaway tests to exercise methods the shipped suite didn't cover (`upsertEntity`/`updateEntity`/`deleteEntity`/`listEntities().byPage()`), and reproduced the RED proof in a genuinely separate disposable worktree rather than reusing the assigned one. Security independently verified via Microsoft's own RBAC documentation that `Storage Table Data Contributor` already grants table-creation actions, not just entity-level CRUD — ruling out this fix introducing a *new* class of production failure the moment it runs. Result: 704 unit + 73 integration tests, mutation 99.17% unchanged (adapters outside Stryker's scope), build clean · merge `6b59839`.

- **2026-07-25 · ⚠️ Production incident found + fixed live: `stfindly` had zero Table Storage tables.** While live-verifying B22's deploy (the established practice tonight of actually exercising the deployed backend rather than trusting green CI), `POST /families` — one of the most fundamental endpoints in the entire app, unchanged since B1 — returned `500 TableNotFound`. `az storage table list --account-name stfindly` confirmed **zero application tables existed at all**. Root-caused immediately: `createTableClient` (`backend/src/adapters/tables/tableClientFactory.ts`) only constructs a `TableClient` pointed at a name — it never creates the table — and Azure Table Storage, unlike the blob store this codebase already handles correctly (create-if-not-exists on append, 002 §3.2), does **not** auto-create a table on first write. Confirmed neither `docs/azure-setup.md` nor `scripts/rename-azure-recreate.sh` (the script that recreated `stfindly` for the Findly rename) ever provisioned the 13 tables specs/002 §2 requires — both only grant RBAC roles and set `TABLES_ENDPOINT`. This had been **silently masked for every read path**: Table Storage's `TableNotFound` is also a plain 404, identical to "row not found," which every existing `isNotFound` check already swallows — so `GET /families/me` correctly returning `PROFILE_NOT_FOUND` all night was accidentally correct for the wrong reason. Writes have no such swallow (nor should they), so every create-path endpoint was broken. **This is very likely the real root cause tying together tonight's original B18 discovery and the B20/B22 findings** — though both remain independently valid, correct defense-in-depth fixes (a table can still legitimately not exist for other reasons), and B21's `clearFamilyMembership` bug is completely unrelated and was genuinely real regardless. **Immediate fix:** all 13 tables created live via `az storage table create` (a safe, additive, non-destructive action — creating empty tables), verified end-to-end against production: `createFamily` (201), device registration (201), **B22's exact scenario** — a family's first-ever locate request (201, correctly `pushFailed` for a token-less device), and `GET /families/me` (200) all now work genuinely correctly. `docs/azure-setup.md` and `scripts/rename-azure-recreate.sh` both corrected with explicit table-provisioning steps so a future infra rebuild can't repeat this silently. **B23** tracks the code-level defense-in-depth fix (self-healing table creation, matching the blob store's existing idiom) — not done tonight, ops-level fix is in place and verified, but relying on manual provisioning forever is exactly the fragility that caused this incident.

- **2026-07-25 · B22** · 1 round · reviews: code **approved** (1 Minor, non-blocking — the branch's "rebased" claim was imprecise, sitting 2 commits behind actual `main` tip; verified via `merge-tree` this caused zero conflict, confirmed at merge), security **approved outright**. The B20 sibling bug, closing the loop that code review opened: `locateRequestsTableRepo.listPendingByTargetDevice` (the push-to-locate coalescing check, 001 §6.1) did a raw unguarded `listEntities()`, so a family's **first-ever** locate request threw `TableNotFound` before the table existed. Fixed by routing through the exact same `collectEntitiesTolerant` helper B20 already built and this same file already uses for `deletePartition`/`deleteRowsForUser` — no new helper, no new swallow logic (both reviewers independently confirmed `listTolerant.ts` itself is untouched by this diff). Security specifically traced that tolerating a missing table here can only ever fall through to "create a new request in the caller's own family/target scope" — `familyId`/`targetDeviceId` remain fully server-derived, never client-widened, so there's no coalescing-across-families path. Code review reproduced the RED proof directly (`git revert` the fix, re-ran, got the exact `TableNotFound` Azurite error) and verified the OData filter is byte-for-byte unchanged — the refactor only changes how results are drained, not what's queried. Result: 704 unit + 69 integration tests, mutation 99.17% (`createLocateRequest.ts` unchanged at 100%), build clean · merge `8453b58`. **Live-verified post-deploy** (see below) — this and B20/B21 close every known instance of the never-created-table bug class found tonight.
- **2026-07-25 · B20 + B21 (post-privacy-wave live-bug fixes — found by actually using the deployed `/delete-account` page, not by any failing test)** · B20: 2 reviews, code found 1 non-blocking documentation gap (closed directly), security **approved outright**; B21: code + security both **approved**, security raised 1 non-blocking advisory. **B20** — `DELETE /users/me` was returning `500 TableNotFound` live on `func-findly`. Root cause: every erasure sequence is list-then-delete, and Azure's `listEntities`/`listBlobsFlat` throw on the **first page fetch** against a table/container that was never created (all storage here is lazily created on first write) — before the per-row delete's own not-found tolerance ever runs. Only `DELETE /users/me` was affected in production, and precisely because it's the one endpoint permitted without a profile (every other endpoint short-circuits on `PROFILE_NOT_FOUND` before touching storage — verified live across `/export`/`/devices`/`/groups`/`DELETE /families/me`). Fixed with shared `collectEntitiesTolerant`/blob equivalents (swallow only 404; negative-control tests prove a 403/500/non-`RestError` still propagates, independently re-run by the reviewer) across every erasure/export adapter — 17 new Azurite integration tests, RED-proof reproduced independently (reverted the fix, watched all 17 fail with real `TableNotFound`). Code review's one finding: the implementing agent *had itself found* a sibling bug (the locate-request coalescing check hits the identical failure mode on a family's first-ever locate request) and correctly scoped it out of the diff — but never wrote that down anywhere, not the backlog, not a spec note, not a comment; its own stated intent didn't survive into the commits. Closed by generalizing specs/002 §2's never-created-table rule (previously worded as specific to the erasure paths; this is the second unrelated endpoint hitting the exact same class in one night, so it's a pattern) and tracking the sibling as **B22**. **B21** — found by the orchestrator while investigating B20 and independently reproduced against real Azurite *before* trusting it as a real bug: `TableUserRepo.clearFamilyMembership` used `Merge` with `familyId: null, role: null` — Azure Table Storage's Merge has no `null` type, so the SDK silently drops null-valued fields; the call "succeeds" (ETag changes) while the old values are completely unchanged. This is what B19's family-deletion footprint and B18's account-deletion cascade rely on to flip surviving members family-less — meaning **every family/account deletion ever run left stale `familyId`/`role`** on affected profiles, pointing at a family whose data was otherwise genuinely deleted. Low real-world exposure (pre-launch, test data only, an account reset is already planned before real users), but the guarantee itself was never real. Fixed: read → ETag-guarded `Replace` preserving `displayName` (verified: `Replace` correctly clears omitted fields, but *also* silently wipes anything else not explicitly included — confirmed by direct reproduction, which is why a bare `Replace` without first reading current state would have just traded one bug for a subtler one) → bounded retry (3, matching `Usage`'s existing idiom) → **throw** (not drop) on exhaustion, since this is a correctness guarantee and not telemetry — the one deliberate divergence from `Usage`'s own convention, and code review specifically validated that decision against the spec's own "idempotent, re-callable" language rather than taking it on faith. Security independently ran the concurrency test against real Azurite (confirmed a genuinely racing `displayName` update survives the retry) and the exhaustion test (confirmed no partial-clear after 3 forced real 412s — `familyId`/`role`/`displayName` all provably untouched). specs/002 §2 gained both general rules (never-created-table tolerance; Merge-cannot-clear-a-field) so this bug class isn't rediscovered per-field or per-endpoint. Result: 704 unit tests (B20) + 688 unit / 49 integration (B21), mutation 99.17%, build clean · merge `cd983c4` (B20).
- **2026-07-25 · W2** · 2 rounds (initial + 1 fix) · reviews: code **1 Minor**, security **1 Major + 2 process**; all closed. The `/delete-account` page (Firebase JS phone sign-in → confirmation → `DELETE /users/me` → `deleteUser()`), which satisfies **Google Play's hard account-deletion requirement**. The agent handled the one genuine design conflict correctly: this page MUST load the Firebase SDK from a CDN, while W1's static-security check exists to guarantee `/g` loads **zero** external resources (the property that makes the join page a no-oracle surface). Rather than loosening the check, it added **opt-in** `--allow-external-prefix`/`--allow-fetch` flags, left the zero-flag invocation byte-identical, and proved it — as did **both** reviewers independently, by injecting violations into scratch copies of `index.html` and confirming they still fail. `XMLHttpRequest`/`WebSocket`/`sendBeacon`/`EventSource` stay banned even on the exempted page, and the Firebase CDN import is version-**pinned** (a `/latest/` import would let Google ship new code into a deletion page silently). All 10 static checks were proven load-bearing by injection; the code reviewer re-verified 6 of them itself. **Security's Major was a gap that pre-dated this task and nobody had noticed:** Azure SWA sets **no** framing protection by default (`globalHeaders` is opt-in), so `/g`, `/privacy` and `/terms` had been framable since W1 — harmless for a join flow, but this page turns a clickjack into irreversible cascading erasure, walking a signed-in victim through disguised clicks including the real SMS code. Fixed site-wide (007 §5 now normative: `X-Frame-Options: DENY` + `frame-ancestors 'none'` as `globalHeaders`), with the H4 AASA content-type interaction checked against Microsoft's docs (different header names merge additively) **and verified live post-deploy** — `/delete-account` 200, both headers present on `/delete-account` and `/g`, AASA and assetlinks still `application/json`. The agent also **proactively flagged its own `AIza…` grep hit** for conscious adjudication rather than hoping it passed; security confirmed it is a Firebase **Web** key (`:web:` appId, client-embedded by design, authorization enforced server-side by ID-token verification) with nothing else credential-shaped alongside — and correctly identified that the checklist had no written carve-out, so it would re-flag this line forever and force every future reviewer to re-derive the same judgment. Carve-out added to `docs/security-review-checklist.md` §1, narrowly scoped. Minor: Firebase-delete error copy split so `auth/too-many-requests` no longer offers a "sign in again" button that would push another SMS into the same rate limit. Result: 4 page checks green, deploy verified live · merge `0797dfb`. **This completes the privacy wave — B17/B18/B19/A8/I8/W2 all done; Play's deletion requirement is satisfied in-app on both platforms plus the web URL.**
- **2026-07-25 · B18** · 2 rounds (initial + 1 fix) · reviews: code **1 Major**, security **1 Blocking + 2 Major**; re-review confirmed all four closed, 2 Minor left open (one recorded as tech-debt above, one a corrected claim). **The Blocking is the most serious defect this gate has caught: a child could erase the entire family's data.** Two parents calling `DELETE /users/me` concurrently each observe the other via a non-atomic `listMembers` and both take the non-cascade branch, leaving a family with **zero parents**; any remaining **non-parent** then satisfied `!familyHasOtherParent` and cascaded, irreversibly erasing every member's history, devices and profiles — though they were neither the last parent nor the sole member. **My spec caused it:** 008 §4.2 said "would leave the family without any parent", which is vacuously true of a family that already has none. Condition narrowed to `(callerIsParent && !otherParentRemains) || isSoleMember`, with the rationale written down (a lingering parent-less family is strictly preferable to a non-admin wiping everyone); the underlying race — the `Families` roster has no ETag guard, a pattern §3.5/§3.6 already share — is tracked as **000 §O19** rather than half-fixed in one endpoint. Code review separately found the **uid-keyed `Usage` partition was never erased**, caused by 008 §2's coverage table and 002 §4.2's numbered list *disagreeing* — the agent implemented the list, which omitted it; not hypothetical, since `apiCalls` is keyed `profile?.familyId ?? uid` (B14), so any groups-only or pre-family period leaves rows a current family member still carries, and Table Storage has no TTL. Security also found the **account-delete analogue of B19's in-flight race** (deleting `Devices` first only stops requests that haven't yet passed their `X-Device-Id` check) and that the **erasure walk was unbounded** (sequential deletes across every history and events day-blob, risking a Functions timeout). All four fixed: guard extended to re-read the device immediately before the writes and abandon the batch, bounded-parallel (16) walks, and an explicit retry contract (a 5xx/timeout means "retry", never "deleted"). **Re-review rigor worth recording:** it reverted each fix in disposable worktrees and measured — finding the agent's "3 tests failed" claim was actually **1**, and explaining algebraically why only one scenario can diverge; it also traced `role` back to the Firebase-verified token to confirm the new condition can't be steered by client input, which would have been strictly worse than the bug being fixed. Result: 688 unit + 43 integration tests, mutation 99.17% (`deleteAccount.ts` and `reportLocations.ts` both 100%), build clean · merge `60522b6`. **This completes the privacy wave's backend (B17/B18/B19); only W2 remains, blocked on H9.**
- **2026-07-25 · I8** · 4 rounds (initial + fix + spec-correction + hardening) · reviews: security **1 Blocking + 2 Minor**, code review **3 Major**, all closed; two re-reviews **approved**. The Blocking was the mirror of Android's: the export — a subject's full 400-day location history in plaintext — was written to `temporaryDirectory` with the subject's real `uid` in the filename and **never deleted**, so a parent's device accumulated one identity-labelled copy per exported child, surviving the deletion of the account it belonged to. Code review found two more real gaps: the export picker listed the **caller** among their own "family members" (001 §13.1 says *another* member), and — the important one — Export/Delete-account were rendered only in the `.loaded` branch, so any `GET /families/me` failure (5xx, timeout, offline) left **no Delete-account entry at all**, breaking 008 §4.4's store requirement; Android's independent load had got this right, making the divergence visible only because both platforms were reviewed against the same spec. Plus the shared Firebase-retry trap (see A8) and a Keychain entry that survived if `signOut()` threw. **Interrupted mid-fix by a session limit**, which left `DeleteAccountScreen.swift` **non-compiling** (calling a removed method, non-exhaustive switch) — caught only because the re-review was told to verify compile-cleanliness from scratch rather than trust the report. **Then the share-sheet spec correction** (see A8) surfaced a latent bug my own fix would have papered over: `removeCurrentArtifact()` keyed off an instance variable a *fresh process* can never have, so a naive cold-start clear would have silently no-opped and the artifact would have survived exactly the case the rule exists for; redesigned to scan by naming predicate, with the clear placed in `FindlyApp.init()` (once per process) rather than `RootView.init()` (reconstructed on every re-render — verified by the reviewer, not assumed). Final hardening round narrowed that predicate from `hasPrefix && hasSuffix` to the exact generated shape (the scan runs at every launch against the **shared** temp directory) and replaced a non-load-bearing safety test whose fixture differed in both prefix *and* extension. **Notable verification:** this host's `swift test` runner cannot execute, so the agent built a throwaway executable SPM package against the built `FindlyKit` to obtain **genuine runtime RED** — reverting the predicate and watching an unrelated file actually get deleted — rather than settling for compile-time or inspection evidence. Merge verified the reviewer's traceability flag directly (the branch predated the spec amendments): `specs/008` is byte-identical before and after the merge. Result: `swift build --build-tests` clean; backend gate re-run post-merge (unaffected, iOS-only): 682 tests · merge `3b5526d`.
- **2026-07-25 · A8** · 3 rounds (initial + fix + spec-correction) · reviews: security found **1 Major + 1 Minor**, code review **2 Major + 1 Minor (spec-hygiene)**, all fixed; re-review **approved**. Security verified the `FileProvider` scope as textbook-correct (`<cache-path>` only, `exported="false"`, read-only temporary grant). The two Majors both concerned data outliving its purpose: exports accumulated in `cacheDir/exports/` with no cleanup and survived account deletion (violating 003 §12.4's "clear **all** local state"), and `LocalStateWiper`'s doc comment asserting it covered all persisted state was demonstrably false. The other Major was a **trap in my spec**: "on failure offer re-authenticate → retry" never said *how*, so the agent shipped a bare retry — but Firebase fails `deleteCurrentUser()` with `requires-recent-login` **after** the backend deletion has irreversibly succeeded, so every retry fails identically and the user is stuck forever, with no sign-out anywhere in the app to escape. Spec fixed first (008 §1.3: **sign out → sign in → re-run delete**, which works only because `DELETE /users/me` is an idempotent no-op for a profile-less caller and the privacy UI is reachable without a profile). Security also found a **path-traversal hardening gap** — the `Content-Disposition` filename was passed unsanitized into `File(exportsDir, name)`, and Java's `File(parent, child)` does **not** normalize `..` (reviewer verified with a standalone Java test); generalised into 008 §3.1 rule 6 so iOS can't repeat it. **The interrupted-session recovery is worth recording:** a session limit killed the fix round mid-flight and the agent transcript did not survive, but the worktree did; a fresh agent inspected the uncommitted WIP and found two things wrong with it that the previous agent's own notes claimed were done — the cleanup triggers were only described in doc comments and never wired, and the traversal test asserted on a local variable never passed to the function under test (so it would have passed against a vulnerable implementation). It restructured the API so the filename has nowhere to leak, and the re-reviewer independently reproduced the vulnerability in a scratch copy (3 of 7 tests fail against the reintroduced bug) rather than trusting the claim. **Final spec correction, escalated from a reviewer's non-blocking FYI:** clearing the artifact on the share-chooser's activity-result callback **races the consuming app** — that callback fires when the target is launched, not when it has read the `content://` bytes, so "Save to Files/Drive" could find the file already deleted. Privacy that silently breaks the feature is a bug, so 008 §3.1 rule 2 was amended to permit only non-racing triggers (clear-before-write, cold start, account-deletion wipe) and the racing launcher/`DisposableEffect` were removed in favour of a `ColdStartExportCleanup` run once per process from `FindlyApplication.onCreate`. Result: 292 tests, 0 failures; backend gate re-run post-merge (unaffected, Android-only): 667 tests, `tsc` clean · merge `48a3cbb`.
- **2026-07-25 · B19** · 2 rounds (initial + 1 fix) · reviews: **both reviewers found a different real Major** — the strongest argument yet for running two independent lenses. **Security found a genuine TOCTOU with GDPR consequences:** `reportLocations.ts` resolves `familyId` from the auth snapshot, reads entitlements once at the top, then appends history several `await`s later with no re-check — so a family deletion completing in that gap lets the create-if-not-exists append **silently recreate location coordinates under the just-erased family**, permanently, since privacy deletion is deliberately one-shot with no sweeper (008 §1.2). Reachable by any ordinary member with a batch in flight. I verified it in source before acting. **This was a spec defect, not just a code defect:** 002 §4.2 claimed the step-1 profile flip stopped ingest "immediately", which is only true for requests that authenticate *after* it — so the spec was fixed first (new normative "write-time family-existence guard": re-verify family existence immediately before the history append and family-keyed usage increment; if gone mid-request, degrade to the family-less path for the remainder). **Code review found the one non-idempotent call** in an otherwise idempotent function: `updateProfile` (steps 1 and 6) throws 404 on a missing profile, unlike every sibling call, so a co-parent concurrently removing a member turns the delete into a raw `500`; the function's own header comment asserting universal idempotency was therefore false. Fixes chosen well: a dedicated idempotent `UserRepo.clearFamilyMembership` port method rather than blanket-swallowing 404 in `updateProfile` (which would mask real bugs in `updateMember.ts`), and a guard reusing the existing `FamilyRepo` point read — no new port, one extra read per batch, short-circuited so family-less callers never pay it. **Re-review verified by reverting the fixes in a throwaway worktree and re-running the new tests against pre-fix code** — both genuinely fail there, so the tests are load-bearing rather than tautological; also confirmed the guard sits after the last intervening `await` (an early re-check would leave the window open) and that `TableUserRepo` swallows only 404 via the existing narrow helper. Merge hit 4 conflicts against B17 in the shared `UsageRepo`/`FamilyRepo` ports, `usageTableRepo` and two fakes — purely additive (B17 read methods vs B19 delete methods), resolved by keeping both; notable trap: both sides ended on a *method signature* sharing a trailing brace, so a naive marker-strip would have produced silently broken syntax. Result: 667 tests (union of both suites), mutation 99.15% (`reportLocations.ts` and `familyDeletion.ts` both 100%), build clean · merge `b493e3c`.
- **2026-07-25 · B17** · 1 round + 1 spec-driven addition · reviews: code + security both **approved outright**, zero findings — the strongest first-round result in this project so far. Both reviewers re-derived independently rather than trusting the report (the code reviewer ran its own scoped Stryker invocation; the security reviewer re-verified the `escapeQuotedString` header handling rather than accepting the claim). Two agent judgment calls were explicitly upheld: `usage: []` for family members is *structurally* correct (002 §2.9 gives family members only family-keyed rows — proved with an adversarial test that seeds a stray uid-keyed row and asserts it still doesn't leak), and the single new surviving mutant (`validate.ts:308`, a zod error-message string) is genuinely equivalent to 7 pre-existing survivors of the same class, since `parseOrThrow` never surfaces `issue.message`. Security threat-model highlights: a crafted `userId` (other family, removed ex-member, arbitrary uid, traversal-shaped, control chars) converges to indistinguishable `403`/`404`/`400` with no existence oracle; the `${familyId}/${userId}/` prefix always terminates in `/` so prefix collision is impossible; the 400-day retention bypass is reachable only from this authorized path (`resolveExportWindow` has exactly one caller and shares no code with `dateRange.ts`); the quota is checked *before* the expensive reads, so a 402 can't be used as a DoS amplifier. **Post-review addition:** I8's security review forced a spec change mid-flight (`Cache-Control: no-store`, 001 §13.1 / 008 §3.1) — the agent verified the spec text existed, cherry-picked *only* the export-hygiene passages onto its branch (correctly excluding the write-time-guard text as B18/B19 territory), then implemented it RED-first. Result: 644 tests, mutation 99.15% (both new domain files 100%), build clean; post-merge integration gate re-run on `main`: 644 tests, mutation 99.15%, `tsc` clean · merge `abd3b71`.
- **2026-07-25 · specs 009 (device runtime) + app-icon handoff integrated (design session, spec+assets only)** — **[specs/009-device-runtime.md](../specs/009-device-runtime.md)**: the cross-platform on-device contract that A9–A12/I9–I12 were unbuildable without (003 §10.5 was explicitly "scaffold only"). Owns: capture triggers + accuracy tiers per `source` (balanced for periodic — never a continuous GPS stream, battery is a hard requirement), the durable queue replacing both in-memory placeholders (1 000-fix cap, oldest-dropped-first, in-flight `PendingBatch` survives process death so retries resend identical content), per-platform scheduling (Android WorkManager ≥15 min / `FOREGROUND_SERVICE_LOCATION` for 5–10 with normative notification copy / once-per-local-day for 1440; iOS as the union of `BGAppRefreshTask` + significant-location-change + geofence transitions with a `×0.8` elapsed rule, and an explicit statement that sub-15-min cadence is *not* achievable), pause as a full teardown with pull-based resume, routing for all four push types (incl. `LOCATE_REQUEST` ignored past `expiresAt + 10 min`, fulfilled even while paused), geofence full-replace re-registration on five triggers with all-transitions-reported-regardless-of-flags, staged permissions with prominent disclosure, and a no-coordinates-in-logs invariant. Pointers added in 003 §9/§10.5/§11 and 004 §7 rather than duplicating (the 006 pattern). **App-icon handoff** (`/tmp/Findly.zip`) integrated as `design/findly-icon/`: **verified the delivered color tokens are byte-identical to those already applied on 2026-07-20** — `diff` of the full Android hex set is clean — so only §4 (the icon) was new, no token re-work. Rasterised the handoff's "still to produce" list with `rsvg-convert` (1024 light/dark/tinted, `play_store_512`, the 40 pt master at @1x/@2x/@3x), **all verified alpha-free** (`sips -g hasAlpha` → `no`), which is the handoff's own #1 "things that will bite" (the App Store rejects a 1024 carrying alpha). Backlog gains **A13** (Android icon integration — assets final, integration only) and I9 gains the iOS asset-catalogue wiring. Noted honestly in the package README: the 80–58 pt intermediate master described in the degradation ladder was **not** delivered and is not derivable by scaling — it needs a design round if wanted.
- **2026-07-25 · store-release roadmap authored** — [`docs/store-release-roadmap.md`](store-release-roadmap.md): M1 (family install via TestFlight + Play closed track) vs M2 (public), five phases, critical path, risk list. Driving finding, **verified in code, not assumed**: every 001-contract/screen task is done but the device runtime is scaffolded — Android `StubPushTokenProvider` + scaffold-only `LocationSyncWorker` (no fused-location capture, no FCM service in the manifest, in-memory fix queue, `PlaceholderMapRenderer`); iOS has protocol seams only and **no `.xcodeproj` app target**. Backlog gains **A9–A12 / I9–I12** (device-runtime wave) so `/dev-loop` can schedule it; H5 note: Play closed track doubles as the ≥12-tester/14-day requirement *and* the family's M1 install channel.
- **2026-07-25 · specs 008 authored (design session, spec-only)** — the privacy-endpoints spec (000 §O7, the last store hard-gate needing design): **[specs/008](../specs/008-privacy-endpoints.md)** (concepts: removal-vs-erasure distinction, data-inventory coverage table, immediate-deletion semantics, web deletion surface) + **001 §13** (three endpoints: unenveloped streaming `GET /export`, `DELETE /users/me`, `DELETE /families/me`; §1.3/§1.5.3/§1.6/§3.3/§3.4/§9/§10/§11 touched) + **002** (§2.1 invite index rows, §2.9 `exports` metric, new §4.2 deletion ordering incl. the interleaved-events filtered-rewrite race guard, §6 checklist) + **000** (D17, O7 resolved, new O18, O11/O14 updates) + client sections **003 §12.4 / 004 §3.6** + specs/README (007+008 rows added — was stale) + store-readiness §0/§1.5. Product decisions locked (2026-07-25, all three recommended options accepted): **self-service web deletion** (`/delete-account`, Firebase JS phone sign-in — SMS is the identity proof an email-request flow can't give), **immediate hard delete** (no recovery window; group-deletion ethos, no tombstone auth states), **last-parent cascade** (account deletion of the only parent deletes the family — no parent-less zombies, erasure never refused). Notable design finds: invites were unfindable at family-delete time (PK = code) → family-side `invite:{code}` index rows + fail-closed §3.4 acceptance; per-subject erasure from interleaved `events/` blobs specified as an `If-Match`-guarded filtered rewrite that provably loses no other member's lines; Firebase user deletion stays client-side (`user.delete()`) preserving D9's no-Google-credential property; export reads the **full 400-day retention**, not the 90-day `historyDays` window (Art. 15). Backlog gains **B17/B18/B19/W2/A8/I8 + H9**; H5 gains the W2 dependency (deletion URL needed for the data-safety form), H8 gains H9 (web App Check before enforcement).
- **2026-07-25 · H7 merge + live backend smoke test (manual session, no /dev-loop agents)** — closed out two things the post-rename docs had wrong or pending. (1) **H7 legal pages merged** (PR #2 `d2b8133`) and verified live: `/privacy`, `/terms`, `/g` all 200 with correct content types. (2) **Discovered the handoff's ⚠️ "backend not deployed" was stale** — a live probe returned `AUTH_MISSING_TOKEN` (deployed), and an unsigned-JWT probe returned `AUTH_INVALID_TOKEN`/"no applicable key in JWKS" (verifier genuinely checking `findly-71f7b`'s keys). (3) **Found the real remaining gap:** Phone sign-in was `OPERATION_NOT_ALLOWED` because the rename spun up a fresh Firebase project and the H2 console steps (Phone provider, test number) were never re-applied to `findly-71f7b`; `createAuthUri` succeeding ruled out the API key. User enabled the Phone provider + test number mid-session. (4) **Smoke test then PASSED end-to-end** via a REST-minted real ID token (`sendVerificationCode`→`signInWithPhoneNumber` `123456` → token `aud/iss=findly-71f7b`, `phone_number=+32470000001`) → `GET /v1/families/me` → `PROFILE_NOT_FOUND`/404 (specs/001 §1.5). (5) **Cleanup:** fixed the Android release `FIREBASE_PROJECT_ID` rename leftover `"CHANGE-ME"` → `"findly-71f7b"` in `mobile/android/app/build.gradle.kts` (verified inert in release — `AuthProviderFactory` only feeds it to `DevAuthProvider`/insecure-local; the real Firebase path reads `google-services.json` — but corrected for clarity + future-proofing). Docs updated (this file). No backend/app code behavior changed; no commit made (working-tree only) pending user's call on committing.

- **2026-07-22 · B14** · 1 round · reviews: code + security both **approved**, no findings. `apiCalls` increment moved out of 28 domain use-cases (each incrementing only on its own success path, the pre-existing B1-era pattern) into the single shared `authenticate()` in `authGuard.ts` — fires once per authenticated request, right after `getProfile(uid)` resolves and before any downstream `PROFILE_NOT_FOUND` throw, under the same `profile?.familyId ?? uid` partition every removed call site used to compute (002 §2.9), and never on a 401 (verified by dedicated tests on both missing- and invalid-token paths). Security review specifically checked for a quota-bypass regression — confirmed no second, un-instrumented auth entry point exists (every `*.functions.ts` handler calls this same `authenticate()`) and that the partition key is derived server-side from the verified `uid`, never a client-supplied claim, so it can't be forged to charge someone else's bucket. Code review verified completeness directly (not just via `tsc` compiling): all 28 call sites removed, all 8 function files rewired to pass `usageRepo`/`clock`, dead `Clock`/`usageDate()` helpers removed only where genuinely unused (4 use-cases correctly kept `usageRepo` for non-`apiCalls` writes). Two flagged behavior changes confirmed as fixes, not regressions: `registerDevice.ts` previously never counted at all (pre-existing gap, now closed); the four bootstrap endpoints now count their very first call under the caller's own `uid` rather than the not-yet-existing family/group, exactly matching 002 §2.9's rule for family-less callers. TDD evidence: 4 new authGuard-level assertions written and confirmed RED before the fix, GREEN after; 28 domain test files updated to drop the now-false per-use-case assertions. Result: 591 tests, mutation 99.13% (`authGuard.ts` itself 100% mutation-killed, 48/48), build clean · merge `44ac5ee`. **This closes out B14/B15/B16/I7 — the full "get me going" tech-debt wave.**
- **2026-07-22 · I7** · 1 round · reviews: code + security both **approved**, no blocking findings (two cosmetic non-blocking nits: a README ASCII-tree alignment nit, and a fallback bundle-id string in `KeychainStore.swift` that doesn't match the real registered `com.findly.ios` — harmless, only fires outside a real app bundle). `FirebaseAuthProvider`'s `verificationID` moved off `UserDefaults`: new `KeychainStoring` protocol + `InMemoryKeychainStore` fake in FindlyKit (6 real `@Test`s covering nil-for-missing-key, exact round-trip, overwrite, remove, remove-of-never-written as a no-op, and key independence), real `Security`-framework `KeychainStore` (app target) using the correct delete-then-add write pattern (avoiding the classic `errSecDuplicateItem` bug) and `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` — strictly more restrictive than the `UserDefaults` plaintext it replaces, appropriate for a short-lived foreground-only session handle. Both reviewers independently re-verified the CLT-only-host `exec format error` claim for the built `.xctest` bundle by actually reproducing it, not just trusting the report. No backlog row existed for this task when the agent started (a known timing gap — the row was added to the working tree but not committed before the agent's worktree branched); it added its own row plus a log entry, which is reconciled into this entry and the backlog table above rather than kept verbatim. `swift build --build-tests` clean; app-target typecheck clean. Result: build clean (Swift package, no backend impact) · merge `4c77b00`.
- **2026-07-22 · B15** · 1 round · reviews: code + security both **approved**, no findings. `toSafeErrorLog` extracted into `src/http` (mirroring `validate.ts`'s established precedent of thin `*.functions.ts` files calling into shared `src/http` logic) — non-`Error` values are `String()`-coerced with zero property access, `Error` instances yield only `message` + an optional string `code`; this removes the prior fragility (relying on Azure `RestError.request/response` happening to be non-enumerable) structurally rather than continuing to depend on it. Wired into all 9 catch-all sites (8 HTTP function files + the sweeper's inline catch, which had a narrower but still-real gap on its non-`Error` branch). Both reviewers independently verified the new wiring tests exercise the real registered handler (mocking the domain dependency to reject with a realistic Azure-SDK-shaped error), not just the helper in isolation — confirmed these would fail against the pre-fix code. Result: 633 tests, mutation 99.17% (`errorLogging.ts` 100%) · merge `2e3dfbf`.
- **2026-07-22 · B16** · 1 round · reviews: code + security both **approved**, no blocking findings. Fix is precise: clears `pushInvalid` only when the incoming `pushToken` is defined AND differs from the stored one — re-supplying the same already-invalid token, or omitting `pushToken` entirely, correctly leaves the flag alone (both explicitly tested, the former specifically preventing a "launder by resending" bypass). One non-blocking security note: a device's own owner could briefly self-suppress their own `pushInvalid` warning with a fake token, but it's self-scoped (no cross-device/family exposure, structurally enforced by partition key, verified by reading the surrounding ownership checks directly) and gets reset on the next real FCM send failure. Result: 622 tests, mutation 99.16% (`registerDevice.ts` scoped 98.55%, one pre-existing unrelated survivor) · merge `f9ee90f`.
- **2026-07-22 · H2** · walked the user through `docs/azure-setup.md` §3's remaining steps live (steps 1–5 — Blaze/budget alert, Phone provider, SMS family-mode allowlist BE+NL, test number — were already done earlier 2026-07-22). **Step 7 (Android app registration):** debug keystore SHA-256 fingerprint obtained via `keytool` (needed a one-time Homebrew OpenJDK install first — the Mac had no JDK at all); registered `com.findly.android` in Firebase with that fingerprint; real `google-services.json` downloaded and confirmed in place (checked directly — real `PROJECT_ID`/`API_KEY`, not the CI placeholder stub). **Step 8 (iOS app registration):** `com.findly.ios` registered; real `GoogleService-Info.plist` confirmed in place (`PROJECT_ID: findly-REPLACEME`, matching `BUNDLE_ID`). APNs key upload deliberately left undone — needs an Apple Developer Team ID that doesn't exist until enrollment clears; owned by H6, cross-referenced there already. **Step 9 (FCM sending key):** user generated and set `FCM_SERVICE_ACCOUNT_JSON` on `func-findly` themselves (kept the live credential out of this session's context entirely, consistent with this project's secret-handling posture); confirmed success, reminded to delete the local key file. **Step 6 (App Check), done last since it needed both apps registered first:** Android registered with Play Integrity, no issues. **iOS hit the same Apple-enrollment wall as everything else Apple-gated in this project** — both App Attest and DeviceCheck require a Team ID Firebase can't get without one; left iOS unregistered rather than faking it, to be finished alongside H6. Net result: H2 is done for everything not gated on Apple enrollment; the two enrollment-gated pieces (iOS App Check, APNs key) now explicitly live under H6, and H8's dependency was updated (`H2` → `H2, H6`) since "App Check enforced on both platforms" can't happen until iOS App Check is even registered. Step 10 (account reset) stays deliberately deferred to pre-launch, as previously agreed.
- **2026-07-22 · H4** · provisioned `swa-findly` (SWA Free, West Europe, resource group `Findly`, subscription "Visual Studio Enterprise Subscription") after confirming this login had genuine access to the existing infra (the session's first `az` login was in the wrong tenant entirely — corporate iFacto Azure, zero visibility into `Findly` — caught before creating anything there). Granted `gh-findly-deploy` (`722f3f16-…`) Contributor on the new resource; set `AZURE_STATICWEBAPP_NAME`/`AZURE_STATICWEBAPP_RESOURCE_GROUP` repo variables. Recorded `JOIN_LINK_HOST = kind-plant-0fb99b003.7.azurestaticapps.net` in both apps' build config (Android's `build.gradle.kts` val; iOS's `AppConfig.swift` — renamed `placeholderJoinLinkHost` → `defaultJoinLinkHost` now that it's the real value, updated the one test asserting the old placeholder was obviously fake). **Discovered mid-task: local `main` was 113 commits ahead of `origin/main` — nothing from this entire `/dev-loop` session had ever been pushed.** Flagged this explicitly and got user confirmation before pushing (a big-bang deploy of the whole specs/005–007 backlog, including the sweeper going live on its production schedule, isn't something to do as a side effect of one task). All 4 pipelines (`backend`/`android`/`ios`/`web-join`) went green on the very first real CI run. **Live verification then caught a real bug:** `apple-app-site-association` served `application/octet-stream` instead of `application/json` — a documented Azure Static Web Apps platform limitation (route-level `headers` can't reliably override `Content-Type` for extensionless files; confirmed via web research, not guessed). Fixed by moving the AASA content to a `.json`-suffixed file with a `rewrite` rule (not `redirect`) mapping the extensionless path Apple's CDN actually requests to it; documented in specs/007 §3 for future maintainers. Re-verified live post-fix: `/g`, `assetlinks.json`, `apple-app-site-association` all `200`, zero redirects, correct content types · commits `77dff51`, `d19c7d6`.
- **2026-07-22 · A6** · 2 rounds (initial + 1 fix) · reviews: security **approved** outright (https fragment parsing confirmed defensive — host/path validated before the fragment is ever read, same whitelist sanitizer as `findly://`, no PII logging, ZXing `core` confirmed encoder-only with zero new permissions); code review found **1 Medium fixed** — the one-time deep-link navigation (`FindlyNavHost`'s `LaunchedEffect(Unit)`) could re-fire on Activity recreation (rotation, dark-mode toggle, multi-window resize, locale change, process-death restore all hand back the same launch `Intent`), forcibly navigating the user back to the join screen and discarding wherever they'd actually navigated to since. Fixed via the standard `savedInstanceState == null` guard on a new `parseIfFreshLaunch` entry point (the already-approved `parse()` core logic untouched). Notably, the agent caught and rewrote its own weak first-draft regression test (one using a mismatched host/path, which would've passed regardless of whether the guard worked) before finalizing — both the fix and the re-review independently confirmed exactly 2 tests are genuinely guard-dependent. Re-reviewed and confirmed closed. Merge hit the predicted trivial conflict in `build.gradle.kts` against A7 (two independent top-level `val` blocks at the same anchor) — resolved by keeping both. Result: 619 tests, mutation 99.16% (backend unaffected) · merge `041e964`. **This merge completes the 5-task convention-groups wave: B13, W1, A6, A7, I6 all done.**
- **2026-07-22 · I6** · 1 round · reviews: code + security both **approved** outright, no findings. The agent self-caught a real bug mid-implementation: its first `matchHttpsJoinLink` draft used `URL.path`, which silently strips a trailing slash, so `https://{host}/g/#CODE` was wrongly accepted instead of rejected (007 requires exact `/g` path match); fixed by validating entirely through `URLComponents`, which preserves the exact path. Both reviewers independently reproduced the bug empirically before trusting the fix — one via a live Swift REPL script, one via a standalone script against the real toolchain — both confirming `URLComponents.path` genuinely preserves `/g/` where `URL.path` doesn't. `GroupJoinQRCode` judged genuinely pure (CoreImage-only, no I/O) and kept real tests rather than treated as untestable framework code. Associated Domains entitlement confirmed to mirror the existing (already-merged) Location Push precedent exactly — commented out, cites H6, no real Team ID. `swift build --build-tests` clean. Backend integration gate re-run post-merge (unaffected, iOS-only change): 619 tests, mutation 99.16%, build clean · merge `ad44b63`.
- **2026-07-22 · W1** · 1 round · reviews: code + security both **approved** outright, no blocking findings (both reviewers independently re-ran the static-security script and injected violations to confirm it's load-bearing; fragment-only code handling, no external resources/storage/analytics, no existence oracle, OIDC-only deploy with the SWA token masked before it ever reaches `$GITHUB_OUTPUT`, no redirect on any route). Two non-blocking notes: the script's network/storage checks are `<script>`-scoped (a future inline-event-handler or CSS `url()` reference wouldn't be caught — not exploited today); the agent's report flagged the iOS bundle id in `apple-app-site-association` as an unverified guess, but code review found it actually matches the value already committed in `mobile/ios/GoogleService-Info.plist` — correct as used, just mis-described in the report. Result: 619 tests, mutation 99.16% (backend unaffected — purely additive `web/` change) · merge `9b56447`.
- **2026-07-22 · A7** · 2 rounds (initial + 1 fix) · reviews: security **approved** outright; code review approved the Gradle/CI wiring outright (independently reproduced by actually running `gradle :app:signingReport` three times — no material/debug fallback, `-P` properties, env vars overriding `-P` — and `actionlint` against the real workflow, both matching the report exactly), found **1 Low fixed** — a docs-only invalid `keytool` command (`-exportcert` + `-list` combined in one invocation, verified to error) in the H5 guidance note; fixed and confirmed against a real throwaway test keystore. Result: 619 tests, mutation 99.16% (build unaffected — Android-only change) · merge `f3fb420`.
- **2026-07-22 · specs 007 + convention pass authored (design session, spec-only, commit `ef79d91`)** — the convention use case (worldwide attendees join a group via one printed QR) drove: **specs/007** (public join links: `https://{JOIN_LINK_HOST}/g#CODE` with the code in the URL **fragment** so the capability never reaches server logs; fully static no-oracle landing page; `.well-known` files for App/Universal Links; on-device-only QR; SWA Free hosting with OIDC-only deploy), **006 §6.3/§7** (SMS region policy split into family mode / open mode — open mode requires App Check *enforced* first, €25 budget tripwire, re-narrow runbook), **cap raise** `maxGroupMembers` 50→200 (backend-flat, D15), 000 D15/D16 + O16 promoted + O7/O10 escalated to pre-launch gates + new O17 (pin clustering), client contracts 003 §12.3 / 004 §3.5. Product decisions locked in (2026-07-22): cap 200; open mode permanent once entered (attestation replaces geography); join-link host = SWA default hostname, custom domain additive later. Docs pass (this commit): `docs/store-readiness.md` (new — Play/App Store gates incl. the O7 privacy-endpoints spec as a hard pre-store gate), azure-setup §3.4 rewrite + new §7 (H4 checklist), security checklist gains 007 capability-URL items, backlog gains **B13/W1/A6/A7/I6 + H4–H8**. Next design session: the privacy-endpoints spec (000 §O7). H2 progress note: steps §3.1–3.5 completed by the user 2026-07-22 (Blaze + alert, Phone provider, SMS family-mode allowlist BE+NL, test number).

- **2026-07-21 · B12** · 2 rounds (initial + 1 fix) · reviews: code review **approved** outright (independently re-ran build/unit/mutation gates itself rather than trusting the report — confirmed the 46-bucket `[today-45..today]` window verbatim against specs/002 §4.1, the timer schedule as a genuine judgment call on an unpinned spec detail, the shared `groupDeletion.ts` extraction as a faithful behavior-preserving refactor of B10's delete logic, and confirmed no CI/deployment changes were needed for the first timer trigger); security found **1 Medium fixed** — a TOCTOU race where a concurrent owner `PATCH`-extend landing between the sweeper's metadata read and its destructive write could irreversibly delete a group the owner had just legitimately extended, since no ETag guarded the writes. Fixed by capturing the group's ETag at read time and gating all three destructive branches (archive-wipe, grace-transition-wipe, hard-delete) on a conditional re-check via a new `assertGroupMetaUnchanged`, skipping (not erroring) on conflict — reusing this codebase's existing ETag-conditional-write idiom (`LastKnown`/`GroupLastKnown`/`Invites`/`Usage`). A real-Azurite integration test proves the guard fires against a genuine concurrent write, not a simulation; 3 unit-level race tests were confirmed load-bearing (verified to go red with the guard bypassed). Re-reviewed and confirmed closed. Result: 619 unit tests + 29 Azurite integration tests, mutation 99.16% (break 60), build clean · merge `87b1ba9`. **This closes the /dev-loop backlog — B1–B12, A1–A5, I1–I5 all done.**
- **2026-07-21 · B10** · 2 rounds (initial + 1 fix) · reviews: code review **approved** outright, resolving the agent's own 2 flagged spec ambiguities as defensible, fully-tested implementation choices (rotate gated like leave/kick, not PATCH — no archived-conflict the way extending `endsAt` has; delete works in any state per 3 independently-agreeing spec sources over a compact summary table's shorthand) with 2 documentation-only footnotes added to specs/005 §2.3 as a fast-follow; security found **1 Medium fixed** — rotating a join code didn't atomically invalidate the old one (`createCode`→`updateGroupMeta`→`deleteCode` as 3 sequential round-trips, with `joinGroup.ts` validating only via the `GroupCodes` lookup), leaving a real window — and on a crash between steps 2 and 3, a **permanent** leak — where both codes stayed joinable, contradicting specs/001 §12.7's "old code stops working instantly." Fixed by making `Groups.meta.code` authoritative in `joinGroup.ts` alongside the existing lookup, collapsing the window to one round-trip and making it self-healing on crash; a new test directly simulates the crash window (creates the new code + commits `updateGroupMeta` without calling `deleteCode`) and confirms the old code is rejected. Re-reviewed and confirmed closed. Result: 598 tests, mutation 99.12% (break 60), build clean · merge `c64b523`.
- **2026-07-21 · I5** · 2 rounds (initial + 1 fix) · reviews: security **approved** outright (the app's first live `findly://` deep link confirmed defensively parsed — whitelist charset, host-disambiguated from the invite link, HTTPS rejected, no other URL data trusted); code review found **3 real findings fixed** — (1) `GROUP_EXPIRED` on the detail screen gave no visible notice, repeating the exact bug Android's A5 review caught and fixed, not carried over to the analogous iOS screen; (2) no confirm dialogs on any owner destructive action, which specs/003 §12.2 now explicitly requires (added as a result of Android's own review round) — the platform-precedent justification that held before A5's spec update no longer applied; (3) most notably, family-less users hit a genuine dead end on Home with no path to Groups at all (`HomeViewModel` mapped every `GET /families/me` failure including `FAMILY_NOT_FOUND` to a generic error with no Groups button) — unlike Android's `HomeStateHolder`, which derives signed-in state purely from auth. Fixed: `GroupDetailViewModel` gained a rendered `.expired` state mirroring the map screen; all six owner actions now route through a `.confirmationDialog`; `HomeViewModel` gained a distinct `.familyless` state for `FAMILY_NOT_FOUND`/`PROFILE_NOT_FOUND` with an unconditionally reachable Groups button (verified to match Android's actual scope — just a Groups button, not a full create/join-family flow). Re-reviewed and confirmed closed. `swift build --build-tests` clean. Backend integration gate re-run post-merge (unaffected, iOS-only change): 526 tests, mutation 99.09%, build clean · merge `a04eb3d`.
- **2026-07-21 · B11** · 1 round · reviews: code + security both **approved**, no findings — position-only `GroupLastKnown` payload verified end-to-end (write and read paths), `isStale` confirmed to freeze `syncIntervalMinutes` at ingest time rather than joining the device's live setting, active-only gating confirmed enforced at write time (not just read-side filtering) via B9's unmodified `deriveGroupState`, membership-before-expiry ordering on the read endpoint masks non-membership and expiry identically, fan-out sourced exclusively from the caller's own `Users` `group:` reverse-index (never client-supplied). Result: 526 tests, mutation 99.09% (break 60), build clean · merge `7fb45dd`.
- **2026-07-21 · A5** · 2 rounds (initial + 1 fix) · reviews: security **approved** outright (deep-link `code` sanitized via a genuine Crockford-base32 whitelist before ever reaching state/network; position-only group map confirmed; owner-only UI gating backed by real backend calls, not client-side-only enforcement; share intent carries only the join code/link); code review found **3 minor fixed** — `AlertDialog` and `Toast` were undocumented second/third exceptions to specs/003 §4.3's "components only" rule (only `DatePickerDialog` was documented), and the map/detail screens gave inconsistent `GROUP_EXPIRED` notices (map showed a real Toast, detail navigated away in under one frame). Fixed by documenting both exceptions in §4.3 first, then mirroring the map screen's Toast into the detail screen; re-reviewed and confirmed closed. Backend integration gate re-run post-merge (unaffected, Android-only change): 495 tests, mutation 99.06%, build clean · merge `07b3cb1`.
- **2026-07-21 · B8** · 2 rounds (initial + 1 fix) · reviews: code review **approved** outright (verified the `deviceIdInUse` weakening was the direct, spec-anticipated consequence of the point-read re-key, not a scope-creep bug; family-less `POST /locations` fix confirmed correct per §1.5 step 4; fan-out correctness traced across 6 call sites with stranger-isolation tests; per-user `maxDevices` and simplified §3.6 cleanup both verified); security found **1 Medium fixed** — the re-key silently dropped within-family `deviceId` uniqueness, letting a malicious family member deliberately register a sibling's known `deviceId` (readable via the already-merged `GET /v1/devices`) and potentially hijack device-targeted operations via `deviceFanout`'s unguarded first-match resolution. Code review had judged the same behavior an acceptable trade-off (near-zero *accidental* UUID collision), but the security reviewer's threat model (deliberate, attacker-chosen collision, not chance) was sharper and is what the fix addresses: restored family-wide uniqueness enforcement at registration time, with specs/001 §1.4/§4.1 and specs/002 §2.4 updated first to describe the new guarantee. Re-reviewed and confirmed closed. Result: 495 tests (after merging with B9), mutation 99.06% (break 60), build clean, auto-merged cleanly against B9's concurrent `repositories.ts` changes · merge `e9f6e4e`.
- **2026-07-21 · B9** · 2 rounds (initial + 1 fix) · reviews: code review **approved** outright (verified error-code count 22→27 directly against pre-branch `errors.ts`, `deriveGroupState` purity/boundaries, `maxActiveGroups`-only-at-create against spec text, owner- vs. caller-scoped plan fields, grace-roster masking, reused CSPRNG join-code generator, no scope creep into B10/B11/B12); security found **1 Medium fixed** — `joinGroupRequestSchema.code` had no Table-Storage-key sanitization (unlike the sibling `groupIdParamSchema` in the same diff), so a malformed code could throw an uncaught `RestError` (noisy 500 + possible partition-key-in-logs) instead of a clean `VALIDATION_FAILED`; fixed by mirroring the existing `.max(16)` + `ALLOWED_TABLE_KEY_CHARS` regex pattern, re-reviewed and confirmed closed. Result: 477 tests, mutation 99.15% (break 60), build clean · merge `32536d1`.
- **2026-07-21 · I3** · 1 round · reviews: code + security both **approved**, no blocking findings. Code review independently verified the two headline claims rather than trusting them: the old `StubAuthProvider` token was reconstructed and confirmed to fail `JSON.parse` in the backend's `InsecureLocalTokenVerifier` (never actually worked), and the new token was reconstructed in Node and confirmed to pass the verifier's exact checks. Also verified: cross-checking Android's already-merged A3 mid-task surfaced two real behavior mismatches (`confirmCode` with no verification in flight now throws `.codeExpired` not `.unknown`; `startPhoneVerification` defensively re-validates) — both fixed via genuine TDD cycles with dedicated tests. `FirebaseAuthProvider.swift` confirmed inert (`#if canImport(FirebaseAuth)`, no SDK present yet). Non-blocking tech-debt from security review, added below: `verificationID` persisted in `UserDefaults` instead of Keychain. `swift build --build-tests` clean. Backend integration gate re-run post-merge (unaffected, iOS-only change): 368 tests, mutation 99.11%, build clean · merge `ae5e8ba`.
- **2026-07-21 · I4** · 1 round · reviews: code + security both **approved**, no findings — wire shapes matched to 001 §12/004 §3.2 field-by-field including position-only group DTOs; a real `Decodable` footgun caught by the agent (inline `= nil` defaults on `Int?` properties silently skip JSON decoding in Swift's synthesized `Decodable`) was independently reproduced and confirmed fixed by the code reviewer via a standalone `swiftc` repro. New group tests correctly added into the existing `@Suite(.serialized) RequestBuildingTests` rather than a new suite, avoiding a repeat of the H1-CI cross-suite `MockURLProtocol` race. `swift build --build-tests` clean. Backend integration gate re-run post-merge (unaffected, iOS-only change): 368 tests, mutation 99.11%, build clean · merge `7a94cf2`.
- **2026-07-21 · B7** · 1 round · reviews: code + security both **approved**, no findings — reviewer independently re-ran the gates on an isolated scratch checkout (not just trusted the agent's report) and reproduced 368/368 tests + mutation 99.11% exactly. Confirmed §4.1 extension was necessary (not scope creep): family-less `listMyDevices`/`patchDeviceSettings` are untestable without family-less `registerDevice` first. Confirmed no cross-tenant collision risk in the shared `Devices`/`LastKnown` port: family partition keys are always `fam_`-prefixed, family-less callers use their own Firebase `uid` (never `fam_`-prefixed) — no re-key needed yet (B8 scope). Non-blocking tech-debt raised by the reviewer, added below: `registerDevice.ts` still doesn't clear `pushInvalid` on a fresh token per §4.1/§8.5 (pre-existing since B1, not introduced by B7). Worktree branched from a stale base (known dev-loop non-determinism) — agent self-corrected with `git reset --hard main`/rebase before starting. Result: 368 tests, mutation 99.11% (break 60), build clean · merge `8a43a15`.
- **2026-07-21 · A3** · 1 round · reviews: code + security both **approved**, no findings (§4.1 state machine matched verbatim, E.164 `PhoneNumberNormalizer`, email/password path confirmed fully deleted via `git grep`, dev-mode auth confirmed release-unreachable via `BuildConfig.AUTH_MODE` hardcoding). Worktree branched from a stale base (known dev-loop non-determinism) — agent self-corrected with `git reset --hard main` before starting. Backend integration gate re-run post-merge (unaffected, Android-only change): 328 tests, mutation 99.61%, build clean · merge `b96f452`.
- **2026-07-21 · A4** · 1 round · reviews: code + security both **approved**, no findings (10 group endpoints wire-matched to 001 §12, 6 new error codes 21→27, `unwrapBare204` DRY refactor verified against pre-refactor auth-retry behavior). Worktree branched from a stale base (known dev-loop non-determinism, see backlog note) — agent self-corrected with `git rebase main` before starting. Backend integration gate re-run post-merge (unaffected, Android-only change): 328 tests, mutation 99.61%, build clean · merge `4091757`.

- **2026-07-21 · specs 005 + 006 authored (design session, spec-only)** — temporary groups (`specs/005`, commit `e6083b7`: 10 endpoints in 001 §12, 4 new tables + per-user `Devices`/`LastKnown` re-key in 002, derived lifecycle + first-ever timer sweeper, live-only/position-only privacy model) and phone-number-only auth (`specs/006`, commit `7da589f`: client-only change, provider-invisible tokens, Blaze/SMS-allowlist/App-Check ops posture, one-time account reset). Product decisions locked in: groups ≤ 50 members / 5 active / 30 days / 7-day grace; SMS allowlist BE+NL+FR+DE+LU; group map position-only; number change = new account. This entry's commit also lands the docs pass: azure-setup §3 rewritten as H2's phone-auth checklist, security checklist gains phone/group items, and the backlog above gains H2/H3 + B7–B12 + A3–A5 + I3–I5.

- **2026-07-20 · Firebase project ID wired up** — user created the Firebase project (console name "Findly", auto-generated **Project ID `findly-REPLACEME`** — confirmed via Project Settings → General, since Firebase's generated ID commonly differs from the typed display name). Set as the `FIREBASE_PROJECT_ID` app setting on `func-findly` (not a secret — same value ships inside `google-services.json`/`GoogleService-Info.plist`, so no confirmation-before-acting concern here, unlike `FCM_SERVICE_ACCOUNT_JSON`). **Still pending (human/secret, docs/azure-setup.md §3):** enable Auth, register Android/iOS apps → `google-services.json`/`GoogleService-Info.plist`, generate + set `FCM_SERVICE_ACCOUNT_JSON`.

- **2026-07-20 · H1 CI pipelines all green + 4 real bugs fixed (`65998a1`)** — after the infra provisioning below, `gh` was used to watch all 3 workflows to a genuinely green state on `main` (not just "should work"). Along the way, running each pipeline for real (most for the *first time ever*) surfaced 5 pre-existing bugs, none introduced by the infra work itself:
  1. **OIDC subject format** — `docs/azure-setup.md`'s federated-credential subject (`repo:owner/repo:ref:...`) doesn't match what GitHub actually presents; the real subject embeds immutable owner/repo IDs (`repo:owner@ownerId/repo@repoId:ref:...`). Fixed via `az ad app federated-credential update`; confirmed by rerunning just the `deploy` job.
  2. **AGP 9 Kotlin plugin conflict** — `org.jetbrains.kotlin.android` is no longer applicable alongside AGP 9's built-in Kotlin support (hard error). Removed from both `mobile/android/build.gradle.kts` and `app/build.gradle.kts`; Compose/serialization sub-plugins untouched.
  3. **iOS Xcode/Swift mismatch on `macos-14` runners** — the default active Xcode's compiler (Swift 5.10) was older than the Testing framework it tried to link (Swift 6.0.3). `ios.yml`'s `package` job now explicitly `xcode-select`s the newest available Xcode 16.x before building.
  4. **Missing import** — `FindlyApiService.kt` used `Envelope<T>` throughout but never imported it (masked until the AGP fix let compilation get that far). One-line fix.
  5. **iOS test race** — `RequestBuildingTests`'s 19 `@Test`s all mutate the same process-global `MockURLProtocol.requestHandler`; Swift Testing runs `@Test`s within a suite concurrently by default (unlike XCTest), so they raced. Added `@Suite(.serialized)`.

  A 6th and 7th bug surfaced on the *first fully green compile+test* of each mobile suite (previously always blocked by #2/#3 above) — genuine app bugs, not environment issues:
  6. **Android: `backgroundScope` + `advanceUntilIdle()` never ran the `init{}`-launched load** in `GeofencesStateHolder`/`HomeStateHolder`/`MapStateHolder`/`SettingsStateHolder` (23 test failures) — confirmed via a minimal local repro (installed a JDK + Android SDK locally via Homebrew to run `./gradlew test` for the first time outside CI). `runCurrent()` reliably drains the same coroutines where `advanceUntilIdle()` doesn't in this project's coroutines-test 1.10.1 setup; swapped in the 4 affected test files. `LocateStateHolderTest` already used `runCurrent()`/`advanceTimeBy()` for an unrelated reason (avoiding racing through intermediate poll states) and was never affected.
  7. **iOS: `GeofencesViewModel.load()` unconditionally reset `state = .loading`** on every call, including refreshes. A `304 Not Modified` response is a no-op in `apply()`, so an unchanged-geofences refresh got stuck on `.loading` forever instead of keeping the prior `.loaded` value — a real UX bug (infinite spinner), not just a test artifact. Fixed to only blank to `.loading` when there's no cached ETag yet (first load).

  Final state: `backend` / `android` / `ios` all green on `main` at `65998a1`. Android: 108/108 unit tests. iOS: 126/126 tests.

- **2026-07-20 · H1 infra provisioned (az + gh)** — resource group **`Findly`** (westeurope): storage **`stfindly`** (Standard_LRS, TLS1.2, no public blob, `history-retention` lifecycle 400d), Function App **`func-findly`** (consumption, **Node 24**, Functions v4, HTTPS-only, system-assigned MI → Storage Table+Blob Data Contributor), app settings `TABLES_ENDPOINT`/`BLOB_ENDPOINT` (no keys). OIDC app reg **`gh-findly-deploy`** (`AZURE_CLIENT_ID` `722f3f16-…`) + federated cred `repo:waldo1001/Findly:ref:refs/heads/main` + `Website Contributor` on the func app. GitHub repo variables set. Branch protection on `main` requires status checks `test`, `mutation`, `android-build`, `ios-package`, `ios-build`. **Node 20→24:** Azure now refuses Node 20 (EOL 2026-04-30) for new Function Apps — provisioned on 24 and bumped `backend.yml` CI to 24 to match. Still say "Node 20" (cosmetic follow-up): project `CLAUDE.md`, `backend/README.md`, specs, `package.json` engines floor (`>=20`, already 24-compatible). **Still human/secret steps:** Firebase project + `FIREBASE_PROJECT_ID` + `FCM_SERVICE_ACCOUNT_JSON` app setting + mobile config files (see `docs/azure-setup.md` §3 and the session's final report for exact commands).

- **2026-07-20 · Design tokens** · applied the delivered "Findly Design System" (`design/findly-design-system/`) into both apps' `DesignSystem` seams — colors (light+dark), unified type scale, spacing, corners; elevation + iOS spacing already matched. Values-only (no logic/component/screen changes); self-verified: iOS `swift build`+`--build-tests` clean, no `Color`/`.system`-font leakage outside `DesignSystem` on either app, values match the WCAG-AA-verified handoff exactly. specs/003 §4.2 + specs/004 §2.1 updated · commit `a878f35`.

**Mobile CI-verified status (2026-07-20 update):** both mobile suites now genuinely build **and** run their full test suite in CI (`android-build`: `./gradlew test`, 108/108 green; `ios-package`: `swift test` on `macos-14`, 126/126 green) — see the H1 CI entry above for the bugs this first real run surfaced and fixed. Still outstanding: the iOS `.xcodeproj` app-target project doesn't exist yet (`ios-build` is still a structure-check stub), and Android release signing isn't configured.

**Known follow-ups / tech-debt (raised in review, deliberately deferred):**
- ~~**`apiCalls` placement.**~~ **RESOLVED (B14).** The increment now lives once in `authGuard.authenticate()`, keyed by `profile?.familyId ?? uid` (002 §2.9), firing regardless of downstream outcome and never on a 401 — 28 per-use-case call sites removed.
- **Catch-all error logging.** Function `errorResponse()` catch-alls log the raw `err` object. No demonstrated leak (Azure `RestError.request/response` are non-enumerable, so coordinates aren't serialized), but harden to log `err.message`/`err.code` only, across all `*.functions.ts`.
- ~~**iOS `verificationID` stored in `UserDefaults`, not Keychain.**~~ **RESOLVED (I7).** `FirebaseAuthProvider` (mobile/ios) now stores the Firebase phone-verification session id via `KeychainStoring` → a real Keychain-backed `KeychainStore` (`kSecAttrAccessibleWhenUnlockedThisDeviceOnly`), not `UserDefaults`.
- **`mapWithConcurrency` masks secondary failures** (`backend/src/adapters/blobs/historyBlobStore.ts`). The bounded-parallel worker pool added in B18 uses `Promise.all`, which settles on the **first** worker rejection — any other worker that also throws is silently discarded. Raised in B18's re-review and **verified with a standalone reproduction** of the exact algorithm (2 failures among 40 items at concurrency 4: only the first surfaces, the second vanishes entirely). **Not** data loss and not a correctness bug — the call still throws, the client still retries, and 002 §4.2's idempotent-retry contract still converges — but during a real erasure failure (e.g. a transient Storage throttling blip hitting several day-blobs at once) the 500 and its log line will name only one of them, making root-cause diagnosis harder than it should be. Fix: `Promise.allSettled` + aggregate all failures into the thrown error before re-throwing. Deliberately deferred as observability-only rather than spending another review round on a merged-clean task.
- **`pushInvalid` not cleared on fresh token.** `registerDevice.ts` doesn't clear `pushInvalid` when a device re-registers with a new `pushToken`, per §4.1/§8.5. Pre-existing since B1 (raised in B7's code review, not introduced by B7) — a device that previously had a push failure marked stays flagged even after a legitimately fresh token arrives, until B8's re-key work touches this path.

- **2026-07-19 · B6** · 1 round · reviews: code + security both **approved** (history read/cursor; **Azurite integration suite ran 18/18**; TLS-downgrade verified LOCAL-ONLY — `allowInsecureConnection` never reaches the `DefaultAzureCredential` prod branch; malicious-cursor injection-safe — flat blob names, `familyId` always first path segment). Deferred 2 §6 integration items: invite single-use race (B3 now merged → nowtestable, add later) + geofence ETag 412→409 (needs B5). Result: 265 tests, mutation 99.53% · merge `a6384b0`.
- **2026-07-19 · B4** · 2 rounds (initial + 1 fix) · reviews: both reviewers found the SAME **1 major, fixed** — `fulfillLocateRequest` was missing the §1.2 `X-Device-Id` ownership check (exploitable: any family member could inject a forged fix into another member's last-known/history); fix adds the `DEVICE_NOT_FOUND` ownership guard before the target-match check, with a dedicated SECURITY test proving the exploit is blocked. FCM §8 credential runtime-only (no key committed). Result: 150 locate tests, mutation 100% on domain/locate · merge `214fbc4`.
- **2026-07-19 · I2** · 2 rounds (initial + 1 fix) · reviews: security **approved** (invite/deep-link validated pre-network; MapKit keyless; no ATS weakening; no PII logging), code review found **4 completeness gaps fixed** — History date-range UI (+client-side 31-day span enforcement), self-management own-row controls (Step down/Leave, server `lastParent` enforced), geofence icon field, device-rename wired in (all with tests). `swift build`/`--build-tests` clean; design seam grep-clean. Test runtime is CI-only (CLT host). · merge `88fc0fc`. **← all coding complete.**
- **2026-07-19 · A2** · 2 rounds (initial + 1 fix) · reviews: security **approved** (maps key config-injected/not committed; no cleartext weakening; invite input safely serialized), code review found **1 medium fixed** — screens were rendering raw `ApiError.message` (server debug text, forbidden by §1.3/§10); now an exhaustive `ApiErrorUserMessage.kt` maps every code→friendly string (mirrors iOS). **A1 retry-duplication carryover resolved** (single `withAuthRetry`). Design seam grep-clean; 3 new design components. Not compiled locally (no toolchain) — CI-compiled. · merge `8193eab`.
- **2026-07-19 · B5** · 1 round · reviews: code + security both **approved** (§1.2 device-ownership on report-events PRESENT — mirrors reportLocations, B4's gap does not recur; parent-only PUT; ETag `"0"`-sentinel/`If-Match`/`412→409` verified against real Azurite; flag-filtered fan-out excludes reporter; frozen null fields for unknown geofenceId; no secrets; cross-family push isolation intact). Non-blocking notes: `apiCalls`-on-all-dup-batch (per-event idempotency makes it defensible — arguably MORE correct than reportLocations' skip) + a cosmetic mid-batch pushInvalid cache-refresh nit. Result: 328 tests, mutation 99.61% (geofence domain 100%), integration 23/23 · merge `741f4b4`. **← backend complete.**
- **2026-07-19 · I1** · 2 rounds (initial + 1 fix) · reviews: code review **approved** — `swift build` + `swift build --build-tests` compile clean on the CLT-only host (test *runtime* runs in CI on `macos-14`); design-swappable seam verified (no `Color(`/`.font(.system)`/hardcoded sizes outside DesignSystem; strict Screens→Components→Theme→Tokens); all 21 error codes; write-only tokens; Location Push entitlement commented-out. Security **approved** (no secrets/config tracked; **1 minor fixed** — added `*.p8` to `mobile/ios/.gitignore`). Not run locally (CLT test-runtime defect) — 55 `@Test`s run in CI. · merge `2144d3f`.
- **2026-07-19 · A1** · 2 rounds (initial + 1 fix) · reviews: code review **approved** (design-swappable seam verified — no hardcoded `Color/dp/sp` outside token dir; full 001 client; all 21 error codes mapped; scope=foundation only), security **approved** (no secrets/config tracked; **3 minor fixed** — cleartext carve-out scoped to `src/debug/` so release is TLS-only, CI `permissions: contents:read`, comment 20→21). Not locally compiled (no Android toolchain) — CI-compiled later. · merge `8bbf4eb`.
- **2026-07-19 · B3** · 2 coding rounds (initial + 1 fix) · reviews: code review **approved** (invite CSPRNG Crockford, race-safe single-use, last-parent protection, §3.6 device cleanup all verified); security found **1 minor fixed** — `userId` path param now zod-validated (rejects empty + Table-Storage-forbidden chars) so a bad key yields `VALIDATION_FAILED` not a masked `INTERNAL_ERROR`; 2 equivalent `refine`-message mutants accepted. Merged behind B2 with a trivial additive `validate.ts` conflict resolved by the orchestrator. Result: 153 tests, mutation 99.51% (break 60), build clean · merge `16115fc`.
- **2026-07-19 · B2** · 2 coding rounds (initial + 1 fix) · reviews: security **approved** clean; code review found **1 major fixed** — `details.fields` now uses bracket notation `fixes[3].recordedAt` (§5.1/§10) instead of dot, killing the deferred B1 `validate.ts` mutant with the correct format — **+1 minor fixed** (`lastKnownTableRepo` retry 2→1 per 002 §2.5); `apiCalls` note deferred to tech-debt above. Result: 89 tests, mutation 100% (365 mutants, break 60), build clean · merge `dad39f9`.

- **2026-07-19 · B1** · 2 coding rounds (initial + 1 fix) · reviews: code+security both ran; **1 finding fixed** — `firebaseJoseVerifier.ts` now enforces §2.2 "iat in the past" (jose omits it without `maxTokenAge`); **1 finding deferred to B2** — surviving `validate.ts` mutant (`.join(".")` vs `.join("")`), unobservable on B1's flat schemas, must be killed by a nested-path test in B2 batch validation · spec change: **specs/001 §4.1 pinned** (omitted token on update = preserve). Result: 46 tests, mutation 99.37% (break 60), build clean · merge `708a6fa`.

Keep this file updated at the end of every session (status table, backlog statuses, log).
